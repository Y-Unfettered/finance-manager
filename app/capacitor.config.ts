import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'app.financemanager.local',
  appName: '财务经理',
  webDir: 'dist',
  android: {
    backgroundColor: '#f4f6f3',
  },
}

export default config
