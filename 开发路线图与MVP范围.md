# 财务经理 APP：开发路线图与 MVP 范围

> 版本：1.0  
> 日期：2026-08-03  
> 首发边界：Android、单人、单账本、本地优先、人民币本位币  
> 本文是开发顺序和版本范围的执行基线；产品细节参见 PRD，视觉参见 UI V3 规范，架构参见技术选型方案。

## 1. 是否已经可以开始开发

**可以开始。** 当前没有阻塞项目脚手架、数据库原型和手工记账闭环的重大产品问题。

| 领域 | 状态 | 结论 |
|---|---|---|
| 产品目标与账户范围 | 已明确 | PRD 已覆盖资产、负债、应收、导入迁移和数据主权 |
| 财务口径 | 已明确 | 支出、收入、转账、还款、充值、借出与收回已有口径 |
| UI 设计系统 | 已明确 | 首页、导航、卡片、字号、颜色、图标与动画已有 V3 Token |
| 技术栈 | 已明确 | Vue 3、Capacitor、Kotlin、SQLite、Vant、Lucide 与测试方案已确定 |
| 自动记账边界 | 已明确 | 通知优先、无障碍增强、OCR/导入兜底；不阻塞 MVP |
| 数据库物理 Schema | 待落地 | Sprint 0 生成首版 DDL 和 migration，不阻塞开工 |
| 钱迹真实导出样本 | 尚未提供 | 不阻塞手工记账和通用导入框架；实现钱迹适配器前需要样本 |
| Android 签名与发布渠道 | 尚未确定 | 不阻塞开发；生成可长期升级的首个安装包前必须确定 |

当前最重要的调整是：原 PRD 的 MVP 范围过大。首个 MVP 不同时开发预算、完整报表、投资持仓、预付卡提醒、周期交易和自动记账，而先完成最小但闭环的数据系统。

## 2. MVP 的唯一目标

MVP 只证明一件事：

> 用户可以把已有账导进来，继续准确记账，随时看到钱在哪里、欠多少、别人欠自己多少，并且数据能够完整备份和恢复。

MVP 必须跑通以下闭环：

```mermaid
flowchart LR
    A["创建本地账本"] --> B["建立账户与期初余额"]
    B --> C["导入历史账或手工开始"]
    C --> D["记录收入 / 支出 / 转账等业务"]
    D --> E["账户、负债、应收自动联动"]
    E --> F["首页与流水核对"]
    F --> G["导出备份并在空库恢复"]
```

不属于这条闭环的能力原则上不进入 MVP。

## 3. MVP 必须完成的功能

### 3.1 本地账本与初始化

- 单人、单账本、人民币本位币、自然月账期。
- 创建账本、选择“导入已有账单”或“从零开始”。
- 新建账户并录入期初余额和余额基准日。
- 内置一套可编辑的基础收支分类。
- 全部核心功能断网可用，不要求注册登录。

### 3.2 账户与资产负债基础

首版统一使用通用账户模型，支持以下类型：

- 现金；
- 银行卡；
- 微信、支付宝等平台余额；
- 公积金、医保、Apple 余额等受限资产；
- 信用卡、白条、先用后付等负债；
- 预付卡；
- 投资账户总市值；
- 借出款等应收资产。

MVP 只要求账户余额和资产分类正确。以下专业能力后置：投资逐笔持仓、行情、预付卡次数和有效期、分期测算、信用卡账单日历。

### 3.3 手工记账

必须支持以下业务动作，并由底层复式分录保证联动正确：

1. 支出；
2. 收入；
3. 账户间转账；
4. 信用消费；
5. 信用卡/平台负债还款；
6. 退款；
7. 借出款；
8. 收回本金与利息；
9. 余额调整。

每笔交易支持：金额、账户、分类、日期时间、商户/对方、备注。标签、附件、地理位置和复杂拆分账后置。

关键口径：

- 转账和还款不计收入或支出；
- 借出本金不计支出，收回本金不计收入；
- 信用消费增加支出和负债；
- 退款冲减原支出；
- 所有余额调整必须生成可查询记录。

### 3.4 首页与流水

- 首页摄影头图和统一顶部操作区；
- 当月收入、支出、结余；
- 按日期分组的流水列表；
- 每条流水显示名称、金额和账户；
- 新增、编辑、删除或撤销后，首页数字和账户余额立即一致；
- 基础搜索：关键词、日期范围、账户和收支方向。

预算卡在 MVP 可以显示“尚未设置”或隐藏；完整预算管理不阻塞发布。

### 3.5 资产、负债与应收总览

- 总资产、总负债、净资产；
- 按账户类型分组展示余额；
- 账户详情与关联流水；
- 应收款列表、新建借出款、分次收回和结清；
- 负债余额与还款记录。

MVP 不做复杂趋势图和未来现金流预测。

### 3.6 导入与迁移

- 通用 CSV 导入；
- 钱迹导出文件适配器；
- 选择来源、字段映射、账户/分类映射、预览、确认；
- 来源交易号或组合指纹去重；
- 显示成功、失败和疑似重复数量；
- 整个导入批次可以撤销；
- 避免期初余额与历史流水重复计算。

钱迹适配器开发前需要一份脱敏真实导出文件和大致数据量。没有样本时先完成适配器接口、通用 CSV 和测试夹具。

### 3.7 数据导出、备份与恢复

- 完整本地备份包；
- 可读 CSV/JSON 导出；
- 备份包含 schema 版本、时间、记录数量和校验值；
- 恢复前备份当前库；
- 能在全新空库中恢复，并校验账户余额和交易数量；
- 数据库 migration 失败时不破坏原数据库。

这是 MVP 必须项，不是发布后再补的设置功能。

## 4. 明确不进入 MVP 的功能

- 通知监听自动记账；
- 无障碍自动识别；
- 截图 OCR；
- 周报、完整月报、年度报告和复杂趋势图；
- 月度/分类预算和超支预测；
- 周期交易、分期计划和智能提醒；
- 投资逐笔持仓、行情和自动估值；
- 预付卡次数、到期和风险折价；
- 自动分类和复杂规则引擎；
- 云同步、多设备同步和服务器账号；
- 多账本、家庭共享和权限；
- 桌面小组件、PDF 报告和主题商店。

这些能力不删除，只进入后续版本。

## 5. MVP 开发里程碑

里程碑按“可验证结果”推进，不按页面数量推进。每个阶段结束都必须能在小米真机上演示。

### Sprint 0：工程与规则地基

目标：项目可以构建、安装、升级数据库，并能展示统一组件。

- Vue 3 + TypeScript + Vite + Capacitor Android 脚手架；
- ESLint、格式化、Vitest、Vue Test Utils 和基础 CI；
- `src/design-system/tokens.css` 与组件展示页；
- `AppTopBar`、`AppIconButton`、`BaseCard`、`MoneyText`、`AccountAvatar`；
- SQLite 连接、schema v1、migration runner、Repository 基类；
- 金额整数分、日期时区和 ID 规范；
- 第一组财务规则测试。

完成门槛：开发版 APK 可安装；组件页与 UI V3 一致；数据库可以从空库创建并重复打开。

### Sprint 1：能记一笔账

目标：完成最短垂直闭环，而不是先把所有静态页面画完。

- 创建本地账本；
- 新建现金、银行卡、平台和信用账户；
- 录入期初余额；
- 支出、收入、转账和信用消费；
- 首页当月汇总和按日流水；
- 编辑、删除、撤销；
- 账户余额自动联动。

完成门槛：在真机上 3 步内记下一笔支出；重启 APP 后数据仍在；转账不计支出，信用消费同时增加支出和负债。

### Sprint 2：账算得对

目标：补齐个人资产负债中最容易记错的交易。

- 还款、退款、余额调整；
- 借出款、分次收回、利息和结清；
- 资产/负债/净资产总览；
- 账户详情和基础搜索；
- 核心余额重建与一致性检查。

完成门槛：用固定测试账套执行所有业务动作后，分录平衡、账户余额和净资产符合预期。

### Sprint 3：旧账能够进来

> 状态：已完成（v0.0.10）

目标：从现有记账 APP 平稳迁移。

- 导入适配器接口；
- 通用 CSV 导入；
- 钱迹文件解析；
- 映射、预览、错误报告、重复检测；
- 导入批次撤销；
- 大数据量和异常文件测试。

完成门槛：同一文件重复导入不会静默产生重复交易；撤销一个批次不影响其他数据。

### Sprint 4：数据不会丢

> 状态：部分完成（v0.0.10）。备份/恢复/导出已完成；应用锁、崩溃测试与真机验收推迟到 0.1.0。

目标：形成可长期自用的 MVP 候选版。

- 完整备份、CSV/JSON 导出和空库恢复；
- migration 前备份和失败回滚；
- 应用锁和最近任务隐私处理；
- 崩溃、断电、低存储和异常退出测试；
- 小米 17 Pro Max / HyperOS 真机视觉与返回手势验收；
- 修复真实数据连续试用发现的问题。

完成门槛：将旧安装的完整备份恢复到全新安装后，交易数量、账户余额、资产负债和导入批次校验全部一致。

Sprint 4 通过后，发布 **MVP 0.1.0**。

## 6. MVP 之后的版本顺序

### 0.2：提高日常使用效率

- 总预算和分类预算；
- 常用模板、周期交易；
- 更完整的搜索筛选；
- 信用卡还款日和到期提醒；
- 预付卡有效期与次数；
- 应收款到期提醒。

### 0.3：减少手工录入

- Android 通知监听；
- 自动捕获队列；
- 待确认账单；
- 去重、账户映射和分类建议；
- 自动记账健康诊断。

### 0.4：复盘与分析

- 周报；
- 完整月报；
- 分类、预算和净资产趋势；
- 可解释洞察；
- 年度汇总基础版。

### 0.5：增强识别与专业资产

- 无障碍增强识别；
- 分享截图和本地 OCR；
- 投资持仓和手工/自动估值；
- 更完整的贷款、分期和现金流预测。

### 1.0：稳定个人生产版

- 完整真实账期验证；
- 性能、耗电、隐私和可访问性验收；
- 自动备份到用户选择的位置；
- 升级、回滚和恢复演练成熟；
- 决定是否需要多设备同步或自托管。

## 7. 每个功能的固定开发流程

每个功能必须按照以下顺序完成，不直接从设计稿跳到页面：

1. **定义用例**：写清楚输入、输出、异常和验收样例；
2. **确认财务口径**：它是支出、收入、资产转移还是负债变化；
3. **数据迁移**：需要新字段或表时先写 migration 和回滚/恢复策略；
4. **领域逻辑与测试**：先测试金额、分录、余额和去重；
5. **Repository / Service**：页面不得直接拼 SQL；
6. **复用组件实现**：只使用 UI V3 Token、`App*` 组件和统一图标；
7. **页面集成**：处理加载、空状态、错误、撤销和返回；
8. **自动化验证**：单元测试、组件测试、关键 E2E；
9. **小米真机验收**：Insets、手势返回、键盘、字体缩放和性能；
10. **更新文档**：记录 schema、组件、已知限制和版本变更。

未经第 4 步验证的财务规则不能直接进入 UI。

## 8. 分支、提交与质量门禁

- 主分支始终保持可构建；功能使用短生命周期分支或小批次提交。
- TypeScript 开启 strict；不使用 `any` 绕过账务类型。
- 金额禁止用浮点数直接计算，统一使用整数分或明确的定点数类型。
- 页面禁止直接访问 SQLite，统一通过 Repository。
- 页面禁止直接写随机色值、圆角、间距和 SVG，统一使用 Token 与组件。
- 每次数据库变更必须带 migration、升级测试和备份兼容说明。
- 每次修复财务口径 Bug 必须先补回归测试。
- 核心测试、类型检查、lint 和 Android 构建失败时不得产出候选安装包。

## 9. MVP 测试账套

建立一套固定的脱敏测试账，至少覆盖：

- 现金支出；
- 银行卡工资收入；
- 银行卡转入微信余额；
- 信用卡消费与分次还款；
- 京东白条消费和还款；
- 预付卡充值与消费；
- 借给朋友并分两次收回；
- 收到本金和利息；
- 一笔退款；
- 一次余额调整；
- 同一 CSV 重复导入；
- 导入后整批撤销；
- 完整备份后在空库恢复。

这套账的期望余额、总资产、总负债、净资产、收入和支出必须预先写死为自动化断言。

## 10. 开工时采用的默认决策

为避免继续等待不影响方向的小问题，开发初期采用以下默认值：

- 首发只做 Android；
- 单人、单账本；
- 数据只保存在本地，MVP 使用手工备份；
- 本位币人民币，账期按自然月；
- 公积金、医保等可计入总资产，但默认不计入可用现金；
- 垫付款与借出本金默认不计个人消费；
- 投资 MVP 只记录账户总市值，不做逐笔持仓；
- 自动记账从 0.3 开始，不阻塞 MVP；
- 实际钱迹导入适配以用户后续提供的脱敏文件为准。

如果后续改变这些决定，使用数据库 migration 和设置项演进，不返工已经正确的账务内核。

## 11. 立即开始的第一项工作

下一步不是继续生成更多静态 UI，也不是先开发自动记账，而是启动 Sprint 0：

1. 创建 Vue 3 + TypeScript + Vite + Capacitor Android 项目；
2. 落地 UI V3 Token 和第一批基础组件；
3. 设计并实现 `ledger / account / transaction / entry / category` 的 schema v1；
4. 用测试账套先证明支出、收入、转账和信用消费能够正确生成分录；
5. 再接入第一个可操作的快速记账页面和首页流水。

做到这里，项目才真正拥有可以持续迭代的“地基”。

