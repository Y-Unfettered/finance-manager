# 财务经理 APP UI 设计规范（小米 17 Pro Max）

> 版本：UI V3.0（设计系统与组件规格版）  
> 日期：2026-08-03  
> 目标：作为 Image 2.0、高保真设计稿和 Vue 3 实际开发的唯一视觉基线

## 1. 机型基线

### 已核实信息

- 目标机型：小米 17 Pro Max。
- 主屏：约 6.9 英寸，1200 × 2608 像素，约 19.6:9，120Hz LTPO AMOLED。
- 正面形态：四边窄边框、圆角直屏、顶部居中前摄开孔。
- 系统环境：HyperOS 3，Android 应用采用 edge-to-edge 全屏布局。

### 设计口径

- Image 2.0 出图：竖屏 1200 × 2608 比例，全屏页面，不套手机模型外壳。
- 开发基准画布：约 393dp × 854dp，仅作设计换算参考。
- 不硬编码状态栏和导航栏高度；开发时读取 WindowInsets。
- 背景和顶部主视觉可延伸至状态栏下面，文字与按钮必须避开状态栏和开孔。
- 底部导航背景可延伸到屏幕底边，图标与文字必须位于手势安全区上方。
- 左右内容边距：目标 393dp 画布固定 16dp；屏幕宽度达到 412dp 及以上时使用 20dp。
- 顶部内容建议安全起点：状态栏 Insets + 12dp。
- 底部内容建议安全终点：导航栏 Insets + 8dp。

## 2. 设计方向

关键词：清爽、可信、克制、温和、精确、长期主义。

视觉定位：介于个人生活工具与专业财务仪表盘之间。避免证券交易软件式的高压红绿、传统银行 APP 的厚重蓝色，以及过度可爱的卡通记账风。

### V3 视觉方向

用户反馈 V1 存在“字号过大、卡片过厚、颜色太满、缺少高级感”的问题。V2 改为：

- 首页顶部使用原创风景摄影图作为情绪主视觉，推荐晨雾山谷、海岸、雪山湖泊、城市天际线等安静题材；图片叠加由下至上的 35% 黑色渐变，保证白字可读。
- 主色只用于选中态、进度条、链接和浮动记账按钮，不再使用大面积纯色资产卡。
- 白色和暖灰白成为绝对主体，摄影图是唯一的大面积彩色区域。
- 卡片从“仪表盘大卡”改为“轻容器”：16dp 圆角、无可见阴影或仅 2% 阴影、1px 极浅边界。
- 信息密度提高约 25%；列表行高由 64dp 调整为 52—56dp；同屏优先显示更多真实流水。
- 首页金额不再使用夸张展示字号，整体更接近日常工具而非营销海报。
- 避免每个分类使用大号彩色圆形图标；流水改为小圆点、简洁线性图标或无图标文本列表。
- 高级感来自摄影、比例、留白、对齐和字重，而不是渐变色块、3D 图标或大量装饰。

核心视觉语言：

- 大面积暖灰白背景，降低长时间查看疲劳。
- 深海青绿色作为品牌主色，表达稳定和资产生长。
- 暖金色只用于预算进度、目标和提醒。
- 收入与支出除颜色外必须配合正负号、箭头或文字标签。
- 卡片留白充分，阴影极轻，以边界和层级为主。
- 图表少而清晰，一屏只强调一个分析结论。

## 3. 色彩 Token

| Token | 色值 | 用途 |
|---|---|---|
| Primary 700 | `#12574E` | 主按钮、重点数字、选中态 |
| Primary 600 | `#176B5D` | 品牌主色 |
| Primary 500 | `#2A806F` | 图表主系列 |
| Primary 100 | `#DCEFEA` | 浅色选中背景 |
| Primary 50 | `#EEF7F4` | 轻提示背景 |
| Accent Gold | `#D8A248` | 预算、目标、温和提醒 |
| Background | `#F4F6F3` | 页面背景 |
| Surface | `#FFFFFF` | 卡片、弹层 |
| Text Primary | `#17211E` | 标题、金额 |
| Text Secondary | `#66726E` | 说明文字 |
| Text Tertiary | `#98A19E` | 辅助信息 |
| Divider | `#E7EBE8` | 分割线、描边 |
| Income | `#248561` | 收入、正向变化 |
| Expense | `#D45F5A` | 支出、负向变化 |
| Warning | `#C9842D` | 临近到期、预算预警 |
| Danger | `#B94343` | 逾期、删除、资金缺口 |
| Info | `#4778AD` | 中性通知、导入信息 |

深色模式后续按语义 Token 适配，不直接反转颜色。

### 3.1 色彩使用比例与规则

- 页面约 80% 使用 Background / Surface，15% 使用文字与分隔层级，品牌主色和状态色合计不超过 5%；摄影图页面除外。
- Primary 600 是默认品牌色；Primary 700 仅用于按下态、深色主按钮或需要更高对比度的位置。
- 大面积背景不得使用 Primary 500–700，主色只用于按钮、进度、选中态、链接和少量关键数字。
- 收入统一 `Income` 并带 `+`；支出统一 `Expense` 并带 `−`；普通账户余额使用 Text Primary，不因正数自动变绿。
- Warning 用于即将到期或预算达到 80%；Danger 用于已经逾期、预算超额、删除和不可逆风险。
- 品牌色只能出现在 `BrandMark` / `AccountAvatar` 内，不能扩散成整张账户卡背景。
- 图表系列顺序固定为 Primary 500、Info、Accent Gold、Income、Expense；同一含义跨页面保持同色。
- 禁用纯黑 `#000000` 正文和纯红 `#FF0000` 警告；摄影图上的白字使用 `#FFFFFF`，并依赖统一遮罩保证对比度。

## 4. 字体与数字

本节为唯一有效字阶，不再同时保留 V1、V2 两套字号。

- 首选字体：MiSans；系统回退：Noto Sans SC、sans-serif。
- Android 设计稿使用 sp；在 Capacitor WebView 中使用同数值 CSS px，并支持系统字体缩放测试。
- 数字统一开启 `font-variant-numeric: tabular-nums lining-nums`，金额必须使用千分位。
- 货币符号比主金额小 25%，与数字基线对齐；负号、正号均不可省略。
- 中文正文默认 Regular；不要用 Light 字重换取“高级感”，否则小字号可读性会下降。

| Token | 字号 / 行高 | 字重 | 固定用途 |
|---|---:|---:|---|
| `type.money-display` | 30sp / 36sp | 600 | 净资产等全页唯一核心金额 |
| `type.money-hero` | 28sp / 34sp | 500 | 首页月支出、月报结余 |
| `type.money-summary` | 20sp / 28sp | 600 | 卡片中的合计金额 |
| `type.page-title` | 20sp / 28sp | 600 | 非导航栏式一级页面标题 |
| `type.appbar-title` | 17sp / 24sp | 600 | 顶部导航标题、月份 |
| `type.section-title` | 16sp / 24sp | 600 | 卡片标题、日期分组标题 |
| `type.list-primary` | 15sp / 22sp | 400 | 流水名称、账户名称、表单值 |
| `type.list-amount` | 15sp / 22sp | 500 | 列表金额 |
| `type.body` | 14sp / 21sp | 400 | 说明、洞察、一般正文 |
| `type.label` | 13sp / 18sp | 500 | 筛选、状态、字段标签 |
| `type.caption` | 12sp / 18sp | 400 | 账户来源、时间、辅助信息 |
| `type.nav-label` | 11sp / 16sp | 500 | 底部导航文字 |

使用限制：

- 一屏最多出现一个 `money-display` 或 `money-hero`。
- 除核心金额外，不使用 24sp 以上字号。
- 卡片标题统一 `section-title`，不得因卡片重要程度临时放大。
- 任何业务正文不得低于 12sp；12sp 只能用于辅助信息。
- 系统字体放大至 1.3 倍时，金额允许换行或缩小一级，但不可截断。

## 5. 栅格、间距、圆角与阴影

### 5.1 间距 Token

| Token | 数值 | 用途 |
|---|---:|---|
| `space.1` | 4dp | 图标与小标签、微调 |
| `space.2` | 8dp | 同一信息组内部间距 |
| `space.3` | 12dp | 卡片之间、字段之间的默认间距 |
| `space.4` | 16dp | 页面边距、卡片内边距 |
| `space.5` | 20dp | 大卡片内边距、标题与正文分区 |
| `space.6` | 24dp | 页面模块之间 |
| `space.8` | 32dp | 大段内容分隔 |
| `space.10` | 40dp | 空状态和顶部大留白 |

- 目标 393dp 宽画布统一使用 16dp 页面左右边距；宽度达到 412dp 及以上时可增至 20dp。
- 卡片垂直间距固定 12dp；只有页面大章节之间使用 16dp 或 24dp。
- 默认卡片内边距 16dp；图表和核心摘要卡可用 20dp。
- 流水列表行高 56dp；紧凑型设置、选择列表最低 52dp。
- 所有可点击区域至少 48 × 48dp，相邻触控区域至少间隔 8dp。
- 所有页面元素必须落在 4dp 网格上；1dp 仅用于边框和分隔线。

圆角：

- 摄影主视觉：底部可不设圆角，或仅底部 20dp。
- 普通信息卡：16dp。
- 紧凑输入框、按钮：12dp；大号主按钮可用 16dp。
- 账户图标容器：12dp。
- 标签和筛选胶囊：999dp。
- 底部弹层顶部圆角：28dp。

阴影：

- 普通卡片：默认无阴影；需要分层时使用 0 1dp 8dp，黑色 2% 透明度。
- 浮动记账按钮：0 8dp 24dp，主色 22% 透明度。
- 优先依靠背景层级和 1px 浅描边，不使用厚重悬浮阴影。

## 6. 图标、品牌标识、插画与图表

### 6.1 图标资源决策

功能图标统一使用 **Lucide 官方 Vue 图标包**，不再混用 Material、IconPark、Font Awesome、Emoji 和 Image 2.0 临时生成图标。Lucide 使用一致的 SVG 结构，支持 Vue、按需引入、尺寸和描边配置，采用 ISC 许可证。

IconPark 虽然支持 Vue 3、Outline、Filled、Two-tone 和 Multi-color，并采用 Apache 2.0 许可证，但本项目不把它与 Lucide 混用；仅在未来确认整套迁移时才可整体替换。

### 6.2 功能图标规格

| 场景 | 图标视觉尺寸 | 容器 / 点击区 | 描边 |
|---|---:|---:|---:|
| 行内辅助、Caption | 16dp | 24dp | 1.75dp |
| 表单、列表次操作 | 20dp | 40–48dp | 1.75dp |
| 顶部栏、底部导航 | 24dp | 48dp | 1.75dp |
| 空状态、功能入口 | 32dp | 48dp | 1.75dp |
| 浮动记账按钮 | 24dp | 56dp | 2dp |

- 默认使用圆角线性 Outline 风格，端点 `round`、连接 `round`。
- 同一页面不得出现不同 stroke width；选中态通过颜色、浅色背景或小范围填充表达，不临时换粗线。
- 图标本体与文字间距统一 8dp；图标按钮不自带额外不可见 margin。
- 所有纯图标按钮必须提供 `aria-label` 和 TalkBack 可读名称。
- 开发层只允许通过 `AppIcon` 组件调用，不允许页面直接粘贴来源不明 SVG。

建议的固定语义映射：

| 语义 | Lucide 图标类别 |
|---|---|
| 返回 / 展开 / 收起 | `ArrowLeft` / `ChevronDown` / `ChevronUp` |
| 更多 | `Ellipsis`，横向；不同时混用竖向三点 |
| 日期 / 账期 | `CalendarDays` |
| 分析 | `ChartNoAxesColumn` |
| 账户 / 钱包 | `WalletCards` |
| 银行 | `Landmark` |
| 信用卡 / 负债 | `CreditCard` |
| 应收款 / 借出款 | `HandCoins` |
| 预付卡 | `Ticket` |
| 投资 | `ChartCandlestick` |
| 隐藏金额 | `Eye` / `EyeOff` |
| 搜索 / 筛选 | `Search` / `ListFilter` |

### 6.3 彩色账户与品牌 Icon

账户图标和普通功能图标分层处理，统一由 `AccountAvatar` 与 `BrandMark` 组件渲染。

- 账户图标外部容器固定 40 × 40dp、12dp 圆角；紧凑流水中可用 32 × 32dp。
- 容器内部图形最大 24 × 24dp，四周至少保留 8dp 安全区。
- 通用银行、现金、信用卡、投资账户使用 Lucide 账户类型图标，采用“单一深色图形 + 10%–12% 同色浅背景”，不做四五种颜色的复杂插画。
- 微信、支付宝、京东金融、具体银行等可识别品牌，优先采用品牌方允许使用的官方 SVG；找不到可靠来源时退回通用账户图标加文字，不从搜索结果随意下载 PNG。
- Simple Icons 可作为品牌 SVG 的候选索引，其图稿仓库采用 CC0 1.0，但每个品牌仍受各自商标与品牌规范约束；使用前记录 `source`、`license`、`guidelines` 和获取日期。
- 品牌图形保持原始比例与颜色，不强制改成 Lucide 描边，也不放进彩色渐变圆球。统一感由相同容器、尺寸和留白实现。
- 页面中品牌图标只用于“账户身份识别”，不能替代按钮、返回、更多等功能图标。

### 6.4 插画与图表

- 首页摄影主题为安静自然景观；不得出现人物、货币堆、金条等俗套图形。
- 空状态仅使用 Lucide 32dp 图标和简短文字，不使用大面积卡通插画。
- 环形图最多 5 个系列，其余归入“其他”。
- 趋势图默认无竖向网格，保留零线和少量横向参考线。
- 所有图表必须配文字摘要和可读数据列表，不能只依赖颜色。

## 7. 全局组件

### 7.1 顶部导航栏

所有二级页面、月报、日/周/月/年统计页统一使用 `AppTopBar`，总高度为：

```text
系统状态栏 WindowInset（动态） + AppTopBar 56dp
```

56dp 只指业务导航栏，不包含状态栏。禁止每个页面自行设置 48、52、60dp 等不同高度。

`AppTopBar` 使用严格三栏结构：

| 区域 | 规格 |
|---|---|
| 左侧槽位 | 56dp 宽；48 × 48dp 点击区；24dp 返回图标 |
| 中间槽位 | 真正屏幕居中；单行；最大宽度 220dp |
| 右侧槽位 | 56dp 宽；48 × 48dp 点击区；最多一个主操作 |
| 标题 | 17sp / 24sp、600；普通页面 Text Primary，摄影图上白色 |
| 底部分隔线 | 默认无；内容滚动到栏下时显示 1px Divider |

如果右侧需要两个以上操作，将次要动作放入 `Ellipsis` 菜单，避免破坏中间标题的绝对居中。

#### `PeriodTopBar`：日 / 月 / 年等统计页

- 左侧固定返回箭头。
- 中间显示当前账期，例如“2026年8月”，文字为 `type.appbar-title`。
- 中间整体为 32dp 高可点击区，月份文字与 16dp `ChevronDown` 间隔 4dp。
- 点击中间打开底部账期选择器；日、月、年维度在选择器顶部切换，不在每个页面发明不同控件。
- 右侧固定一个与当前页面有关的操作：筛选、日历、分享或更多；视觉尺寸 24dp、点击区 48dp。
- 左、中、右位置在所有页面完全一致，不能因右侧图标不同让标题偏移。

#### 背景变体

- `surface`：白色或页面背景，用于普通业务页。
- `transparent`：覆盖在摄影图上，几何尺寸与 `surface` 完全相同。
- `scrolled`：摄影图或内容滚走后变为白色 Surface，并出现 1px 分隔线。
- 变体只改变背景和图标颜色，不改变高度、padding、字号或槽位。

首页可不显示返回按钮，但顶部菜单、月份和右侧操作仍需落在同一套 56dp 垂直节奏中。

### 7.2 底部导航

五项：`首页`、`资产`、中央 `记账`、`分析`、`我的`。

- 导航内容高度 64dp，加底部系统 Insets。
- 中央记账按钮 56dp，主色圆形，白色加号；比导航栏上浮 12dp。
- 当前项使用主色图标和 12sp Medium 文字；非当前项使用 Text Tertiary。
- 记账按钮不依赖文字也要有无障碍名称“记一笔”。

### 7.3 卡片系统

所有卡片由 `BaseCard` 派生，禁止页面临时指定圆角、阴影和随机 padding。

| 组件 | 尺寸 | 内边距 | 用途 |
|---|---:|---:|---|
| `BudgetCard` | 96dp 高 | 水平 16dp、垂直 14dp | 首页预算摘要 |
| `CompactCard` | 最小 56dp 高 | 16dp | 折叠分组、单行入口 |
| `SummaryCard` | 最小 104dp 高 | 20dp | 合计、趋势、双指标 |
| `DailyLedgerCard` | 内容自适应 | 水平 16dp | 日期分组流水 |
| `ChartCard` | 内容自适应 | 20dp | 趋势和分类图表 |

统一属性：

- 圆角 16dp，背景 Surface，默认无阴影，边框 1px Divider。
- 卡片间距 12dp；页面首张卡与顶部内容间距 16dp。
- 标题位于左上，使用 16sp / 24sp、600；右侧最多一个操作。
- `DailyLedgerCard` 日期头高度 44dp，流水行高 56dp，分隔线从正文起点对齐。
- 卡片可以整体点击时，不再添加多个弱箭头；只有确实能展开的组件使用 Chevron。
- 卡片高度由组件变体决定，不根据页面空白随意拉长。

`BudgetCard` 内部固定节奏：标题行 22dp、10dp 间距、8dp 进度条、8dp 间距、20dp 底部指标。进度条圆角 999dp，空轨道 `Primary 100`，实际进度 `Primary 500`。

### 7.4 按钮

- 主按钮：48dp 高，主色实心，白字。
- 次按钮：48dp 高，Primary 50 背景，主色文字。
- 危险按钮：默认描边或文字按钮，确认后才使用危险实心。
- 同一页面只出现一个主按钮。

### 7.5 输入

- 数字金额是快速记账页面第一焦点。
- 选择器优先用底部弹层，不用居中小弹窗。
- 输入框高度 52dp；错误同时显示图标、描边和文字。

### 7.6 隐私

- 顶部金额区提供眼睛按钮，一键隐藏全部金额。
- 隐藏后显示 `••••••`，图表保留相对形状但隐藏精确值。
- 最近任务缩略图默认模糊财务数据；应用内截图开关放在安全设置。

## 8. 页面母版

### 8.1 首次启动与迁移

- 顶部品牌名“财务经理”和抽象资产流线插画。
- 标题：“先把你的财务底账理清”。
- 三个价值点：数据留在自己手里、资产负债一目了然、账单随时可迁移。
- 主按钮：“导入已有账单”；次按钮：“从零开始”。
- 底部说明：“本地优先，不连接支付账户”。

### 8.2 首页

- 首页摄影主视觉 `HomeHero` **固定总高度 246dp**，从屏幕物理顶部开始，包含状态栏和透明顶部操作区；目标 393 × 854dp 画布中约占 28.8%。
- V2 母版中的摄影区原为 296dp；真机反馈最终调整为 246dp。若按 1200px 宽出图，摄影区高度约 751px。
- 开发使用 `height: 246px; box-sizing: border-box; padding-top: env(safe-area-inset-top)`；不要使用模糊百分比。
- 图片使用 `object-fit: cover`，默认焦点 `50% 50%`；每张照片可记录独立 `object-position`，不得通过拉伸适配。
- 图片叠加统一遮罩：顶部 18% 深色保护状态栏，中部基本透明，底部逐渐增加到 68% 黑青色，确保所有白字在不同照片上均可读。
- 状态栏下依次为月份、隐藏金额、最大数字“本月支出”、收入与结余双列。
- 英雄区底部数据距摄影区底部 24dp；月支出标签与主金额间隔 4dp，主金额与收入/结余行间隔 12dp。
- 摄影区结束后留 16dp，再进入预算卡；页面左右边距 16dp。
- 预算卡固定为 96dp 的 `BudgetCard`，只显示标题、进度、剩余和总额。
- 首页不单独放大型分类图表，支出分析放入分析页，给流水留出更多空间。
- 预算卡与第一张流水卡间距 12dp；各日期卡之间同样为 12dp。
- 流水按日期分组，每日一个白色轻卡；日期头 44dp、流水行 56dp，显示小圆点、名称、账户和金额。
- 底部导航固定，中央记账按钮突出。

### 8.3 快速记账

- 顶部类型分段控件：支出、收入、转账、还款。
- 居中大金额 `¥ 0.00`。
- 常用分类 4 × 2 图标网格。
- 账户、日期、备注为三行选择项。
- 底部数字键盘与“保存”主按钮，单手可达。

### 8.4 资产总览

- 顶部净资产卡：净资产、总资产、总负债、可用现金。
- 下方按“流动资金、受限资产、预付资产、投资、应收款、负债”分组。
- 每组显示合计、占比和 2—3 个账户，支持展开。
- 右上角“对账”作为重要操作。

### 8.5 负债与还款

- 顶部显示总欠款、未来 30 天应还、可用现金覆盖率。
- 横向还款日历展示未来 7 天。
- 卡片区分已出账、未出账、分期；每张卡显示到期日和状态。
- 资金缺口使用浅红提示条，不满屏红色。

### 8.6 预付卡

- 顶部账面余额与保守估值双指标。
- 风险筛选：全部、即将到期、长期未用。
- 卡片显示商户、余额/次数、有效期、最近使用日、风险状态。
- “充值”和“消费”是卡片详情中的两个明确动作。

### 8.7 投资

- 顶部总市值、累计投入、持仓盈亏和估值时间。
- 下方资产配置环图与持仓列表。
- 涨跌使用颜色 + 正负号；不模仿交易软件分时行情。
- 主操作为“更新市值”，买入/卖出放在更多操作中。

### 8.8 预算

- 月份切换、总预算圆环、剩余金额、日均可花。
- 分类预算使用横向进度条；80% 暖金、100% 珊瑚红。
- 显示“预计月底”虚线标记。
- 底部主按钮“调整预算”。

### 8.9 月报分析

- 月报摄影主视觉 `ReportHero` 固定总高度 256dp，包含系统状态栏和透明 `PeriodTopBar`；顶部栏规格与普通页面完全相同。
- 顶部账期使用 `PeriodTopBar`，中间显示“2026年8月”，右侧为分享或更多；不得另做一套高度。
- 摄影区显示“本月结余”核心数字，内容距底部 20dp。
- 四个摘要：收入、支出、结余率、净资产变化。
- 趋势折线、分类排名、预算偏差各一张卡。
- 首屏给一句可解释洞察，可点击查看依据。

### 8.10 待确认

- 顶部分段：全部、导入、计划、自动识别。
- 批次卡显示来源、条数、疑似重复和时间。
- 单笔项目可修改分类和账户。
- 底部固定“确认选中”按钮；支持整批撤销。

### 8.11 搜索

- 顶部大搜索框，下方常用筛选胶囊。
- 结果摘要显示筛选范围内收入、支出和净额。
- 列表与首页流水使用同一组件。
- 空状态提供清除筛选，不用大面积插画。

### 8.12 我的/设置

- 顶部“财务经理”与本地账本状态。
- 最重要卡片显示最近备份时间、立即备份、恢复演练。
- 设置分组：账本、分类与标签、导入导出、安全隐私、外观、关于。
- 危险操作“清除数据”单独放在最底部。

## 9. Image 2.0 生成纪律

- 第一张生成首页，作为母版参考图。
- 后续页面必须引用首页母版，锁定配色、字体、圆角、图标和底部导航。
- 每次只生成一个完整页面，不将多张手机屏幕拼成概念海报。
- 页面必须是正视、平面、无透视的高保真 UI；不出现手、桌面、手机外壳和环境背景。
- 使用真实中文短标签和真实金额；禁止乱码、伪文字、水印和第三方 Logo。
- 状态栏时间统一 `9:41`，电量 `82%`；底部使用手势导航条。
- 示例月份统一 `2026年8月`，避免跨页数据冲突。
- 所有页面采用同一组示例账户：招商银行、微信余额、现金、京东白条。
- 同一组示例数据：月收入 `¥18,600`、月支出 `¥7,846.50`、结余 `¥10,753.50`。

## 10. 无障碍与开发验收

- 普通文字对比度至少 4.5:1，大号文字至少 3:1。
- 图标、图表等非文字关键信息与背景至少 3:1。
- 所有触控目标至少 48 × 48dp。
- 支持系统字体放大，关键数字不被裁切。
- 颜色不是收入、支出、预警的唯一表达方式。
- 图表提供摘要、数据列表和 TalkBack 描述。
- edge-to-edge 下顶部开孔、状态栏、三键导航和手势导航均不遮挡操作。
- 金额隐藏、应用锁、备份状态在所有核心页面保持一致。

### 10.1 设计一致性验收

- 页面中不得出现设计 Token 之外的字号、颜色、圆角和间距；特殊值必须先进入规范再使用。
- 所有顶部栏截图叠加比较时，返回图标、中间标题和右侧操作的中心线误差不得超过 1dp。
- 所有功能图标必须通过 `AppIcon`；所有账户图标必须通过 `AccountAvatar` 或 `BrandMark`。
- 不允许使用 Emoji、系统字符箭头或临时 PNG 代替已有矢量图标。
- 同类卡片圆角、边距、描边、标题位置必须完全一致。
- 设计稿交付时标注使用的组件名与变体，不只交付一张无法测量的 PNG。

### 10.2 Vue 3 Token 基线

Capacitor WebView 在正确 viewport 配置下，以下 CSS px 按设计 dp 使用：

```css
:root {
  --size-home-hero: 246px;
  --size-report-hero: 256px;
  --size-app-bar: 56px;
  --size-bottom-nav: 64px;
  --size-touch-target: 48px;
  --size-icon-inline: 16px;
  --size-icon-control: 20px;
  --size-icon-navigation: 24px;
  --size-account-avatar: 40px;

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;

  --radius-control: 12px;
  --radius-card: 16px;
  --radius-sheet: 28px;
  --radius-pill: 999px;

  --color-primary-700: #12574e;
  --color-primary-600: #176b5d;
  --color-primary-500: #2a806f;
  --color-primary-100: #dcefea;
  --color-primary-50: #eef7f4;
  --color-background: #f4f6f3;
  --color-surface: #ffffff;
  --color-text-primary: #17211e;
  --color-text-secondary: #66726e;
  --color-text-tertiary: #98a19e;
  --color-divider: #e7ebe8;
  --color-income: #248561;
  --color-expense: #d45f5a;
  --color-warning: #c9842d;
  --color-danger: #b94343;
}
```

项目创建后将这些变量移动到 `src/design-system/tokens.css`，组件和页面只能引用变量，不直接写十六进制色值和随机尺寸。

## 11. 参考依据

- 小米 17 Pro Max 公开机型资料：6.9 英寸、1200 × 2608、120Hz。
- 小米澎湃 OS 开发者平台：MiSans、圆角、安全区、深色模式、大字体与无障碍适配指导。
- Android Developers：edge-to-edge、WindowInsets、48dp 触控目标和颜色对比度指导。
- Lucide 官方 Vue 指南与 ISC License：https://lucide.dev/guide/vue
- Simple Icons 品牌 SVG 与 CC0 1.0 仓库：https://github.com/simple-icons/simple-icons
- IconPark Vue 3、多主题与 Apache 2.0 说明：https://github.com/bytedance/IconPark
