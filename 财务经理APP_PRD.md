# 财务经理 APP 产品需求文档（PRD）

> 目标版本：v0.3.0「可用性重构版」
> 文档版本：V2.0
> 状态：待产品确认
> 更新日期：2026-08-08
> 适用平台：Android 优先，移动端竖屏优先
> 产品定位：本地优先、数据自主可控的个人记账与资产管理工具

---

## 1. 本次改版结论

本次版本不再继续堆叠零散入口，而是围绕“记一笔、看本月、管预算、查分类、看账户、核资产”六条核心路径重构产品。

本版本一次性交付以下能力：

1. 精简侧边栏，只保留“我的账本、搜索、账单、设置”。
2. 保留已经认可的首页背景图和月度收支总览，补齐最近汇总、日历账单和可跳转的交易详情。
3. 重做预算管理，完整支持三种预算模式、一级分类预算和按月自动复制。
4. 补齐一级/二级分类管理、分类图标和分类统计页。
5. 重做资产页、账户详情、账户统计、信用账户账期和账户图标。
6. 保留支出、收入、转账、信用消费、还款、退款等必要账务能力，但不单独建设分期、报销和存钱计划。
7. 所有金额口径在首页、预算、账单、分类统计、账户统计和资产统计之间保持一致。

本版本的视觉和交互参考 `UI参考图` 中的钱迹页面，但只借鉴信息结构和交互方式，不复制其品牌、头像、图片或受版权保护的专属素材。

---

## 2. 产品目标与非目标

### 2.1 产品目标

- 日常支出在 10 秒内完成记录。
- 用户进入首页后，立即看懂当月收入、支出、结余和预算余量。
- 点击任意分类或账户，都能继续查看对应统计和交易明细，不再出现“只能看、不能继续点”的断路。
- 信用卡、花呗、京东白条等账户能正确展示欠款、额度、账期和还款信息。
- 所有资产、负债、预算和统计均由同一套交易数据计算，避免多处数字不一致。
- 新月份无需重新设置预算，系统自动沿用上月配置。
- 所有核心功能离线可用，数据可备份、恢复和导出。

### 2.2 本版本明确不做

以下功能从界面、路由和产品文案中移除，不保留占位页面：

- 意见反馈
- 关于
- 存钱计划
- 分期与周期分期
- 报销管理及“报销”快捷入口
- 社区、会员、广告、运营活动
- 自动读取银行卡、支付密码或短信验证码
- 通知监听自动记账、OCR、云端 AI 分类
- 多人共享账本

投资账户可以作为资产账户记录余额和流水，但本版本不做行情、持仓、收益率和投资建议。

---

## 3. 当前版本基线与主要差距

### 3.1 可以直接保留的能力

- 左上角三杠图标和侧边栏抽屉交互。
- 首页背景图、当前月份、月支出、月收入和月结余区域。
- 按日分组的交易流水卡片。
- 本地账本、账户、分类、交易、导入导出、备份恢复和应用锁基础能力。
- 支出、收入、转账、信用消费、还款、退款的底层账务类型。
- 资产总额、负债和净资产的基础计算。

### 3.2 本版本必须解决的差距

- 侧边栏入口过多，包含不需要的功能。
- 预算页只有基础能力，没有三种模式和明确的分类外支出口径。
- 首页缺少最近汇总卡片、卡片设置和日历账单视图。
- 分类名称可以展示，但分类不可继续进入统计。
- 分类只具备基础创建能力，缺少图标、层级、排序和二级分类维护。
- 账户详情信息不足，信用账户没有账期、账单月和还款信息。
- 资产与账户图标不足，辨识度低。
- 分析页仍是占位页，统计入口与业务上下文断开。
- 账单、分类统计、账户统计、资产统计尚未统一筛选器和统计口径。

---

## 4. 信息架构与导航

### 4.1 一级结构

| 入口 | 作用 | 进入方式 |
|---|---|---|
| 首页 | 月度总览、预算、最近汇总、每日流水 | 默认启动页 |
| 资产 | 净资产、资产负债、账户分组、借入借出 | 首页右上角资产图标或首页横向切换 |
| 记账 | 支出、收入、转账 | 全局悬浮加号 |
| 侧边栏 | 我的账本、搜索、账单、设置 | 首页左上角三杠 |

不再设置独立的“分析”占位主入口。分类统计从分类进入，账户统计从账户进入，资产统计从资产页进入。

### 4.2 侧边栏最终结构

侧边栏自上而下仅展示：

1. 我的账本
2. 搜索
3. 账单
4. 设置

侧边栏顶部展示当前账本名称，可点击进入“我的账本”。不展示会员、使用天数、意见反馈、关于、存钱计划、分期和报销管理。

### 4.3 页面路由建议

| 页面 | 建议路由 |
|---|---|
| 首页 | `/` |
| 资产 | `/assets` |
| 记账 | `/transactions/new` |
| 我的账本 | `/ledgers` |
| 搜索 | `/search` |
| 账单 | `/bills` |
| 设置 | `/settings` |
| 预算管理 | `/budget` |
| 分类管理 | `/categories` |
| 分类统计 | `/categories/:categoryId/statistics` |
| 账户详情 | `/accounts/:accountId` |
| 账户统计 | `/accounts/:accountId/statistics` |
| 资产统计 | `/assets/statistics` |
| 借出管理 | `/receivables` |
| 借入管理 | `/payables` |

---

## 5. 全局视觉与交互规范

### 5.1 视觉原则

- 保留现有首页背景图和清爽卡片风格。
- 页面背景使用浅灰，内容使用白色圆角卡片。
- 标题、金额、辅助文字形成三级层级；同类标题样式必须统一。
- 支出使用克制的红色，收入使用绿色，转账/还款使用中性色或蓝色。
- 金额默认保留两位小数并使用千分位。
- 不依赖颜色单独表达状态，同时配合正负号、文字或图标。

### 5.2 图标规范

- 分类图标：使用统一线性/面性图标库，支持选择图标和主题色。
- 分类列表图标：浅色圆形背景，中央图标水平垂直居中，图标颜色比背景深。
- 账户图标：内置现金、银行卡、微信、支付宝、花呗、京东白条、美团月付、抖音月付、公积金、医保和常见银行图标。
- 未匹配账户使用通用图标或账户名称首字，不显示空白占位。
- 不直接打包参考应用的专有图标文件；银行和平台标识需使用可合法使用的本地资源或自制等价图形。

### 5.3 通用交互

- 所有看起来可点击的卡片必须有真实目标页。
- 页面加载时使用骨架或占位，不用空白闪屏。
- 空数据必须说明原因，并提供“记一笔”或“添加”操作。
- 删除交易、账户或分类前二次确认；无法删除时明确说明关联数据。
- 图表点击后显示具体数值和日期/分类，不只展示装饰图形。
- 筛选条件离开页面后保留到本次会话结束；重新进入默认使用上次选择。

---

## 6. 首页

### 6.1 顶部月度总览

沿用已认可的背景图布局，展示：

- 当前年月，可点击打开年月选择器。
- 月支出（主金额）。
- 月收入和本月结余。
- 左上角三杠菜单。
- 右侧依次提供日历视图、统计入口和资产入口。

年月选择器支持按年、按月切换。默认账单周期为自然月；信用账户自己的月账单按其账单日计算，不改变首页自然月统计。

### 6.2 预算摘要卡片

首页预算卡片展示：

- 标题“预算”。
- 预算进度条。
- 剩余金额。
- 总预算金额。
- 右侧三个点进入预算卡片设置或预算管理。

点击卡片主体进入预算管理页。未设置预算时展示“未设置”，点击后进入预算设置。

### 6.3 最近汇总卡片

预算卡片下方新增最近汇总模块。

默认标题为“最近 7 日汇总”，内容包括：

- 柱状图：按日展示支出，或同时展示收入与支出。
- 卡片底部两行：收入金额、支出金额。
- 右上角三个点：打开本卡片快捷设置。

设置项：

| 设置 | 可选值 | 默认值 |
|---|---|---|
| 显示类型 | 仅支出、收入与支出 | 收入与支出 |
| 日期范围 | 本周、最近 7 日、最近 15 日、不显示 | 最近 7 日 |

日期定义：

- 本周：周一 00:00 至当前时刻。
- 最近 7 日：包含今天在内的连续 7 个自然日。
- 最近 15 日：包含今天在内的连续 15 个自然日。
- 不显示：整个卡片从首页移除，首页其他卡片自动上移。

同一设置也必须在“设置 > 首页展示”中提供。

### 6.4 每日流水

- 按日期倒序分组。
- 日期卡片标题展示当日收入和当日支出。
- 每条记录展示分类、二级分类或备注、账户和金额。
- 支出为负值红色，收入为正值绿色，转账/还款为中性色。
- 点击记录打开交易详情。
- 点击分类名称或分类行进入分类统计。
- 点击账户名称进入账户详情。

### 6.5 日历视图

点击顶部日历图标进入当前月份日历：

- 每个日期格展示当天收入和支出摘要。
- 支持“收支、结余、收入、支出”四种显示方式。
- 日历下方显示所选月份的总收入、总支出和结余。
- 点击某一天后，下方显示当天流水。
- 可继续切换年月。
- 若存在信用账户，提供“本月信用账单”入口，进入信用账户账单汇总。

---

## 7. 账单与搜索

### 7.1 账单页

“账单”是完整流水浏览页，不是信用卡分期页。

必须支持：

- 列表视图和日历视图切换。
- 按年、月选择账单范围。
- 按收入、支出、转账、信用消费、还款、退款筛选。
- 按账户、一级分类、二级分类筛选。
- 展示当前筛选范围的收入、支出和结余。
- 点击任意记录进入交易详情。

### 7.2 搜索页

支持组合搜索：

- 关键词：分类、备注、账户名称。
- 金额范围。
- 日期范围。
- 交易类型。
- 账户。
- 一级/二级分类。

结果按日期分组，显示命中数量和汇总金额。无结果时可清空筛选，不创建空占位功能。

---

## 8. 快速记账与交易详情

### 8.1 主记账界面

顶部只保留三个主要标签：

1. 支出
2. 收入
3. 转账

信用消费不是独立的用户学习入口：在“支出”中选择信用卡、花呗、京东白条等负债账户时，底层自动记录为信用消费。还款从信用账户详情或转账页面进入。退款从原支出详情发起。

### 8.2 支出/收入

页面组成：

- 顶部一级分类图标网格。
- 选中一级分类后，可继续选择其二级分类。
- 备注输入。
- 金额输入和数字键盘。
- 账户、日期、图片凭证、优惠金额等常用附加项。
- 保存按钮颜色随收入/支出类型区分。

不展示“报销”和“分期”快捷项。

保存后的联动：

- 支出更新当月支出、账户余额、预算和分类统计。
- 收入更新当月收入、账户余额和分类统计。
- 信用消费增加当月支出和信用账户欠款，不减少现金资产。

### 8.3 转账

- 必填转出账户、转入账户和金额。
- 可交换转出/转入账户。
- 可选日期、备注、图片、手续费和优惠。
- 转出和转入账户不可相同。
- 转账本金不计收入、不计支出；手续费计支出。

### 8.4 还款

- 从信用账户详情的“还款”按钮进入，默认带出目标信用账户。
- 用户选择付款账户、还款金额、日期和备注。
- 还款减少付款账户资产与信用账户欠款。
- 还款不再次计入支出。

### 8.5 退款

- 仅从原支出详情发起。
- 默认关联原交易，可填写全部或部分退款金额、退款账户和日期。
- 退款减少原分类和原预算周期的已支出金额。
- 跨月退款按退款发生月影响账户余额，同时在原支出所属预算统计中保留关联说明；统计筛选中可查看退款来源。
- 退款不得超过原交易未退款金额。

### 8.6 交易详情底部卡片

根据类型展示操作：

| 类型 | 操作 |
|---|---|
| 支出/信用消费 | 复制、退款、编辑、删除 |
| 收入 | 复制、编辑、删除 |
| 转账 | 复制、编辑、删除 |
| 还款 | 复制、编辑、删除 |
| 退款 | 查看原交易、编辑、删除 |

详情字段包括金额、分类、账户路径、时间、备注和图片。分类行可进入分类统计，账户行可进入账户详情。

---

## 9. 分类体系与分类统计

### 9.1 分类层级

- 收入分类和支出分类分别管理。
- 最多两级：一级分类和二级分类。
- 预算只设置到一级分类。
- 二级分类的交易自动汇总到其一级分类预算和一级分类统计。
- 一级分类可以没有二级分类，此时交易直接记在一级分类上。

### 9.2 分类管理

支持：

- 新增一级分类。
- 为一级分类新增二级分类。
- 编辑名称、图标和颜色。
- 长按排序。
- 启用、停用。
- 将现有一级分类降级为另一一级分类下的二级分类。
- 存在交易的分类不允许直接物理删除，只能迁移交易后删除或停用。

新增分类时提供可搜索图标库。批量新增可以保留为辅助能力，但不作为主流程。

### 9.3 分类统计页

页面标题格式为“分类统计 - 日用品”，标题下显示当前统计年份或日期范围。

页面结构：

1. 日期范围切换与筛选。
2. 指标卡：总收入、总支出、结余、交易笔数、日均/笔均金额。
3. 按月柱状图，支持收入/支出切换。
4. 二级分类或相关分类的环形占比图。
5. 按月分组的交易明细。

筛选器支持：

- 本月
- 上月
- 今年
- 去年
- 指定年份
- 全部
- 自定义起止日期

点击柱状图月份后，明细只显示该月份；再次点击取消。

---

## 10. 预算管理

### 10.1 概念定义

设：

- `T`：总预算。
- `C`：所有已设置一级分类预算之和。
- `S_selected`：已设置预算的一级分类支出之和。
- `S_other`：未设置分类预算的其他支出之和。
- `CountedSpend`：计入总预算的支出。

仅支出和信用消费计入预算；收入、转账本金、还款、借入借出本金不计入。退款冲减对应支出。

### 10.2 三种预算模式

#### 模式 A：同时设置总预算和一级分类预算

要求 `T >= C`，保存时若 `T < C` 必须阻止并提示。

1. 当 `T = C`：
   - `CountedSpend = S_selected`
   - 未设置分类预算的其他支出不计入总预算。
   - 未分配预算为 0。
2. 当 `T > C`：
   - 未分配预算 `U = T - C`。
   - `CountedSpend = S_selected + S_other`。
   - 未分类预算余量 `URemaining = U - S_other`。
   - 分类外支出先消耗未分配预算，超出后总预算显示超支。

#### 模式 B：只设置总预算

- 当月所有支出均计入总预算。
- `CountedSpend = S_selected + S_other`，此时不需要分类预算。

#### 模式 C：只设置一级分类预算

- 总预算自动等于 `C`。
- `CountedSpend = S_selected`。
- 未设置分类预算的其他支出不计入总预算。

### 10.3 月份复制规则

- 预算配置默认开启“自动复制到下月”。
- 新月份首次打开预算或产生第一笔交易时，系统从最近一个有配置的月份复制预算模式、总预算和分类预算。
- 复制的是配置，不复制已支出金额。
- 修改预算时提供“仅本月”和“本月及以后”两个范围。
- 删除某个分类预算时同样提供“仅本月”和“本月及以后”。
- 分类被停用时，历史预算保留，未来月份不再自动复制该分类。

### 10.4 预算页布局

#### 总预算卡片

- 左侧第一行大字“总预算”。
- 左侧第二行小字：“总额 ¥x | 支出 ¥y”。
- 右侧圆环表示已使用百分比。
- 圆环中间两行：“剩余”和金额。
- 超支时圆环超过 100% 按 100% 填满，并显示“超支 ¥x”。

#### 分类预算标题卡片

- 左侧第一行大字“分类预算”。
- 左侧第二行显示分类预算合计。
- 右侧加号添加一级分类预算。

#### 分类预算列表

每行三列：

1. 左列：浅色圆形背景和对应分类图标。
2. 中列：一级分类名称；下方显示“预算总额、交易笔数、实际支出”。
3. 右列：圆环百分比；圆环中央两行显示“剩余”和金额。

点击整行进入该分类统计页。长按或更多菜单支持修改金额和删除预算。

### 10.5 预算边界规则

- 预算百分比 = `CountedSpend / T`；预算为 0 时不计算百分比。
- 分类百分比 = 该一级分类支出 / 该分类预算。
- 交易编辑分类、金额或日期后，旧月份和新月份预算都必须同步重算。
- 删除交易或退款后，预算实时回退。
- 二级分类支出归集到所属一级分类。
- 当月无预算时首页显示“未设置”，不显示 0% 误导用户。

---

## 11. 资产总览

### 11.1 顶部资产卡片

展示：

- 净资产 = 计入资产统计的总资产 - 计入资产统计的总负债。
- 总资产。
- 总负债。
- 金额隐藏/显示按钮。
- 更多菜单。

### 11.2 资产趋势摘要

- 展示资产项目数、负债项目数和负债率。
- 负债率 = 总负债 / 总资产；总资产为 0 时显示“--”。
- 点击进入资产统计页。

### 11.3 借入借出

- 总借入、总借出摘要卡片。
- 点击分别进入借入管理和借出管理。
- 列表展示对方、日期、原金额、已归还/收回和剩余金额。
- 支持新增、部分归还/收回、结束债权债务和查看已结束记录。
- 借入借出本金不计收入或支出；无法收回/无需归还时，必须通过明确的损失/收入调整交易处理。

### 11.4 账户分组

按类型分组展示：

- 资金账户：现金、银行卡、微信、支付宝等。
- 信用账户：信用卡、花呗、京东白条、月付等。
- 受限资产：公积金、医保等。
- 其他资产：预付、投资余额、其他。

分组可折叠；分组标题显示合计金额。每个账户行显示图标、名称、余额或欠款，点击进入账户详情。

### 11.5 资产页快捷操作

右下角悬浮按钮展开：

- 添加账户。
- 账户分组管理。
- 显示/隐藏已隐藏账户。

不提供分期入口。

---

## 12. 账户创建、修改与详情

### 12.1 账户分类选择

添加账户时先选择类型和品牌图标：

- 资金账户：现金、微信、支付宝、银行卡、公积金、医保、数字人民币和其他。
- 信用账户：信用卡、花呗、京东白条、美团月付、抖音月付和其他信用账户。
- 其他资产：预付余额、投资余额和自定义资产。

选择银行卡后可继续选择常见银行；没有匹配项时使用“其他银行”。

### 12.2 通用字段

所有账户都有：

- 账户类型。
- 账户名称。
- 图标和主题色。
- 初始金额/初始欠款。
- 创建或余额基准日期。
- 是否计入资产统计。
- 是否在记账页显示。
- 备注。

“是否计入总资产”在界面统一命名为“是否计入资产统计”，避免负债账户被误解为正资产。关闭后，该账户仍保留流水，但不进入总资产、总负债和净资产计算。

### 12.3 信用账户专属字段

- 总额度。
- 当前欠款：由初始欠款、信用消费、退款、还款和余额调整计算，不允许在日常编辑中静默覆盖。
- 出账日：1—31 日。
- 还款日：1—31 日。
- 可选还款提醒提前天数。

若某月份不存在 29、30、31 日，账单日或还款日顺延/回落规则统一采用该月最后一个自然日。

### 12.4 信用账期规则

账单日属于当前账单周期的最后一天。

例如出账日为 20 日：

- 2026-03-21 00:00 至 2026-04-20 23:59:59 的信用消费，归入 2026 年 4 月账单。
- 2026-04-21 00:00 至 2026-05-20 23:59:59 的信用消费，归入 2026 年 5 月账单。

账单金额 = 周期内信用消费 - 周期内关联退款；还款不改变账单消费额，只改变欠款和还款状态。

若修改出账日，只重算未关闭账单和后续账单；历史已确认账单保留原周期，并记录修改时间。

### 12.5 账户详情页

普通资产账户顶部显示当前余额；信用账户顶部卡片显示：

- 当前欠款。
- 总额度。
- 可用额度。
- 出账日期。
- 还款日期或下一还款日。

顶部操作：

- 统计。
- 修改。
- 更多。

信用账户额外提供固定底部“还款”按钮；所有账户提供“记一笔”。不展示分期按钮。

下方按账单月/自然月分组：

- 普通账户按自然月展示流入、流出和交易明细。
- 信用账户按账单周期展示月份、周期起止日期、本期消费、本期退款、本期还款和交易明细。

### 12.6 账户更多

展示：

- 初始金额或初始欠款。
- 创建时间。
- 是否计入资产统计。
- 隐藏/显示。
- 归档。
- 删除。

账户存在余额、欠款或交易时不得直接删除，应引导归档或先处理余额。

---

## 13. 账户统计与资产统计

### 13.1 账户统计

从账户详情的“统计”进入。

页面包括：

1. 总流入、总流出、净流入、转账/还款金额、交易笔数。
2. 按月柱状图。
3. 分类环形图。
4. 按月交易列表。

信用账户的“流出”表示消费和费用，不把还款重复算作消费；还款金额单独展示。

筛选范围与分类统计一致：本月、上月、今年、去年、指定年份、全部、自定义日期。

### 13.2 资产统计

从资产页的资产趋势卡片进入。

页面支持查看：

- 净资产。
- 总资产。
- 总负债。

页面结构：

- 年份切换和筛选。
- 净资产/资产/负债切换。
- 按月趋势折线图。
- 每月卡片：月末净资产、净资产变化、总资产和总负债。

统计以每月最后一笔交易后的余额为月末值；无交易月份沿用上月月末值。

筛选支持指定年份、年份范围、全部和自定义起止月份。

---

## 14. 我的账本与设置

### 14.1 我的账本

- 展示当前账本及其本位币、创建时间和交易数量。
- 支持新建、切换、重命名和归档账本。
- 默认账本不可在仍有数据时直接删除。
- 不同账本的账户、分类、预算和统计完全隔离。
- 本版本不做多人共享和跨账本合并报表。

### 14.2 设置页

设置页分组：

1. 首页展示
   - 最近汇总显示类型。
   - 最近汇总日期范围或不显示。
   - 首页金额是否默认隐藏。
2. 记账设置
   - 默认支出账户、默认收入账户。
   - 分类管理。
   - 记住上次使用账户。
3. 预算设置
   - 自动复制到下月开关，默认开启。
4. 数据管理
   - 导入、导出、备份、恢复。
5. 安全
   - PIN、应用锁、生物识别（设备支持时）。
6. 外观
   - 跟随系统、浅色、深色。

设置页不出现意见反馈、关于、报销、分期和存钱计划。

---

## 15. 统一财务口径

### 15.1 交易影响表

| 业务 | 资产/负债影响 | 收支影响 | 预算影响 |
|---|---|---|---|
| 现金/银行卡支出 | 资产减少 | 计支出 | 计入适用预算 |
| 信用消费 | 负债增加 | 计支出 | 计入适用预算 |
| 收入 | 资产增加 | 计收入 | 不计预算 |
| 账户转账 | 一增一减 | 不计收支 | 不计预算 |
| 信用还款 | 资产减少、负债减少 | 不计收支 | 不计预算 |
| 退款 | 资产增加或负债减少 | 冲减支出 | 冲减对应预算 |
| 借出 | 现金减少、应收增加 | 不计支出 | 不计预算 |
| 收回借款 | 现金增加、应收减少 | 不计收入 | 不计预算 |
| 借入 | 现金增加、应付增加 | 不计收入 | 不计预算 |
| 归还借款 | 现金减少、应付减少 | 不计支出 | 不计预算 |
| 手续费/利息 | 资产减少 | 计支出 | 计入费用分类预算 |
| 余额调整 | 调整账户余额 | 不计普通收支 | 不计预算 |

### 15.2 不可违反的约束

- 转账、还款和借入借出本金不得重复计入收支。
- 删除或编辑交易必须同步回滚账户余额、预算、账单和统计。
- 金额使用人民币分的整数存储，界面再格式化为元。
- 同一笔信用消费只属于一个账单周期。
- 同一笔退款必须关联原支出，且累计退款不超过原支出。
- 所有页面必须通过相同查询口径计算，不得各自维护独立的展示数字。

---

## 16. 核心数据模型调整

### 16.1 主要实体

| 实体 | 关键字段 |
|---|---|
| Ledger | id、name、currency、createdAt、archivedAt |
| Account | id、ledgerId、type、brandKey、name、iconKey、color、openingBalanceMinor、includeInAssetStats、visibleInEntry、status |
| CreditProfile | accountId、creditLimitMinor、billDay、repaymentDay、reminderDays、effectiveFrom |
| CreditStatement | id、accountId、statementYearMonth、periodStart、periodEnd、amountMinor、refundMinor、repaidMinor、status、confirmedAt |
| Category | id、ledgerId、direction、parentId、name、iconKey、color、sortOrder、status |
| Transaction | id、ledgerId、type、occurredAt、amountMinor、categoryId、accountId、toAccountId、originalTransactionId、note、status |
| BudgetConfig | id、ledgerId、yearMonth、mode、totalBudgetMinor、autoCopy、sourceMonth |
| CategoryBudget | id、budgetConfigId、categoryId、amountMinor |
| HomeSummaryPreference | ledgerId、displayType、rangeType |
| Receivable/Payable | id、ledgerId、counterparty、originalMinor、settledMinor、occurredAt、dueAt、status |

### 16.2 数据迁移要求

- 升级前自动创建可恢复备份。
- 现有一级分类迁移后保持一级分类，现有交易关联不变。
- 现有信用账户迁移到 `CreditProfile`；未知额度和日期保持为空，不虚构默认值。
- 现有“是否计入总资产”迁移为 `includeInAssetStats`。
- 已存在的预算迁移成最接近的预算模式，并在首次打开预算页时提示用户确认。
- 移除入口不等于删除历史数据；报销、分期等旧数据若存在，只在历史账单中只读显示，不再允许新建。
- 迁移必须幂等，失败时恢复升级前备份。

---

## 17. 页面状态与异常处理

所有新增页面必须覆盖：

- 加载中。
- 无数据。
- 有数据。
- 查询失败。
- 数据迁移未完成。
- 金额隐藏。
- 账户或分类已归档。

关键异常文案：

- 总预算小于分类预算之和：“总预算不能小于分类预算合计 ¥x”。
- 退款超额：“退款金额不能超过剩余可退金额 ¥x”。
- 删除有关联数据的账户：“该账户仍有余额、欠款或交易，请先处理后归档”。
- 删除有关联交易的分类：“该分类已有交易，请先迁移交易或停用分类”。
- 账单日不存在：“本月按最后一天计算”。

---

## 18. 非功能需求

| 维度 | 要求 |
|---|---|
| 离线 | 记账、查询、预算、账户、统计、备份导出核心能力离线可用 |
| 性能 | 10 万笔交易下，首页首屏可交互时间小于 2 秒；普通筛选结果小于 500ms |
| 一致性 | 单笔交易保存、编辑、删除使用数据库事务，不产生半条交易 |
| 图表 | 图表查询按日/月预聚合或索引，不在 UI 主线程全量扫描 |
| 可访问性 | 支持系统字号、触控区域至少 44×44、图标有无障碍名称 |
| 隐私 | 不上传账单明细；日志不记录金额、完整备注和账户隐私字段 |
| 兼容性 | 优先适配小米 17 Pro Max 设计基准，同时覆盖常见 Android 竖屏和安全区 |
| 可维护性 | 预算、账期、余额和统计规则必须有自动化单元测试 |

---

## 19. 验收标准

### 19.1 侧边栏与导航

- 侧边栏仅有“我的账本、搜索、账单、设置”。
- 被删除功能在侧边栏、设置、记账和账户详情中均无入口。
- 首页、资产、账单、分类统计和账户详情之间不存在无法返回或无目标的点击项。

### 19.2 首页与最近汇总

- 首页背景图和月度收支正常展示。
- 最近汇总能正确切换“仅支出/收入与支出”。
- 本周、7 日、15 日的边界日期符合定义。
- 选择“不显示”后卡片立即消失，设置页可重新开启。
- 日历视图每天的金额与同日交易列表合计一致。

### 19.3 预算模式测试用例

1. 总预算 3000，餐饮 1000，交通 500，其他支出 200：
   - `T > C`，总预算已用金额包含 200。
   - 未分配预算为 1500，未分配剩余为 1300。
2. 总预算 1500，餐饮 1000，交通 500，其他支出 200：
   - `T = C`，总预算已用金额不包含其他支出 200。
3. 只设置总预算 3000，任意分类支出均计入总预算。
4. 只设置餐饮 1000、交通 500：
   - 总预算自动为 1500。
   - 其他分类支出不计入总预算。
5. 新月份自动复制预算配置，已支出金额从 0 开始。
6. 一笔支出从餐饮改为交通后，两类预算立即重算。
7. 对原支出退款后，分类预算和总预算同步减少已用金额。

### 19.4 分类与统计

- 可以新增一级分类、为其新增二级分类并选择图标。
- 二级分类交易会计入一级分类预算和统计。
- 点击首页交易的分类可进入正确分类统计页。
- 分类统计筛选、指标、柱状图、环形图和明细合计一致。

### 19.5 信用账户与账单周期

- 信用账户可设置账户类型、名称、额度、初始欠款、出账日、还款日和是否计入资产统计。
- 出账日 20 日时，3 月 21 日至 4 月 20 日交易全部进入 4 月账单；4 月 21 日交易进入 5 月账单。
- 信用消费增加支出与欠款；还款减少欠款但不增加支出；退款减少欠款和原消费。
- 账户详情正确展示当前欠款、总额度、可用额度、出账日、还款日和按账期分组的流水。
- 点击账户统计后，消费和还款分别统计，不重复计算。

### 19.6 资产与账户

- 每个内置账户类型都有图标，无空白图标。
- 关闭“计入资产统计”的账户不会进入总资产、总负债和净资产，但其流水仍可查询。
- 资产统计每月末值与当月最后余额一致，无交易月份正确沿用上月。
- 借出、收回、借入和归还本金不会影响普通收入支出。

### 19.7 数据可靠性

- 升级前备份可恢复。
- 交易新增、编辑、删除后，首页、账户、预算、分类统计和资产统计在同一刷新周期内一致。
- 核心规则测试全部通过后才能发布。
- 旧数据迁移失败时不会损坏升级前数据。

---

## 20. 实施顺序

本版本按以下依赖顺序开发，但作为同一个版本整体交付：

1. 数据模型迁移、统一账务口径、分类层级和信用账期计算。
2. 侧边栏与路由精简、设置结构调整。
3. 快速记账、交易详情和分类/账户跳转闭环。
4. 预算三模式、自动复制和预算页重构。
5. 首页最近汇总、账单列表和日历视图。
6. 资产总览、账户创建/修改、信用账户详情和借入借出。
7. 分类统计、账户统计和资产统计。
8. 数据迁移、性能、回归测试和真机验收。

任何阶段都不得以占位页面作为“完成”。页面必须具备真实数据、空状态、错误状态、跳转和验收用例后才算完成。

---

## 21. 发布门槛

满足以下条件才允许发布 v0.3.0：

- 本 PRD 的 P0 页面和验收项全部完成。
- 不存在意见反馈、关于、存钱计划、分期和报销管理的新建入口。
- 自动化测试覆盖预算三模式、账期边界、还款、退款、转账和资产统计。
- lint、类型检查、单元测试、Web 构建和 Android 构建全部通过。
- 在至少一台真实 Android 设备上完成：首页、记账、预算、分类统计、信用账户、还款、资产统计、备份恢复的完整冒烟测试。
- 版本升级可保留旧账本、账户、交易、分类和现有设置。
- 发布包附带版本说明、APK 校验值和可恢复备份说明。

---

## 22. 本轮产品决策记录

以下事项已经确认，不再作为待办问题：

- 首页现有背景图和月度收支区域保留。
- 侧边栏仅保留我的账本、搜索、账单、设置。
- 意见反馈、关于、存钱计划、分期和报销管理全部删除。
- 预算必须支持三种模式并自动复制到新月份。
- 预算按一级分类设置，二级分类向上归集。
- 首页增加可配置的最近汇总卡片。
- 分类和账户都必须可以点击进入对应详情/统计。
- 信用账户必须支持额度、欠款、出账日、还款日和按账期查看。
- 出账日属于上一周期的结束日，例如 3 月 21 日至 4 月 20 日归入 4 月账单。
- 账户提供“是否计入资产统计”开关。
- 账户和分类必须有可辨识图标。
- 本版本不做分期。

后续若产品讨论产生新决定，应先更新本 PRD，再进入实现，避免代码、界面和统计口径再次分叉。

---

## 附录 A：UI 参考图覆盖清单

| 参考图 | 对应 PRD |
|---|---|
| 钱迹-侧栏.jpg | 4.2 侧边栏最终结构 |
| 钱迹-首页.jpg | 6.1、6.2、6.4 首页 |
| 钱迹-首页-有最近7日汇总卡片.png | 6.3 最近汇总卡片 |
| 钱迹-首页-有最近7日汇总卡片-设置.png | 6.3、14.2 最近汇总设置 |
| 钱迹-首页-日历视图.png | 6.5 日历视图 |
| 钱迹-首页-账单筛选年月.png | 6.1、7.1 年月与账单筛选 |
| 钱迹-首页-每项记录点击后的卡片-支出详情.png | 8.6 支出详情 |
| 钱迹-首页-每项记录点击后的卡片-收入详情.png | 8.6 收入详情 |
| 钱迹-首页-每项记录点击后的卡片-还款类.png | 8.4、8.6 还款详情 |
| 钱迹-首页-每项记录点击后的卡片-分类统计页.png | 9.3 分类统计 |
| 钱迹-首页-每项记录点击后的卡片-分类统计页-筛选.png | 9.3 分类统计筛选 |
| 钱迹-首页-每项记录点击后的卡片-z账户页.png | 12.5 账户详情 |
| 钱迹-首页-每项记录点击后的卡片-z账户页-资产统计页.png | 13.1 账户统计 |
| 钱迹-首页-每项记录点击后的卡片-z账户页-资产统计页-筛选.png | 13.1 账户统计筛选 |
| 钱迹-资产.jpg | 11 资产总览 |
| 钱迹-资产-资产视图.png | 13.2 资产统计 |
| 钱迹-资产-资产视图-筛选.png | 13.2 资产统计筛选 |
| 钱迹-资产-借出管理（借入同）.png | 11.3 借入借出 |
| 钱迹-资产-添加.png | 11.5 资产快捷操作 |
| 钱迹-资产-添加-分类.png | 12.1 账户分类选择 |
| 钱迹-资产-账户每项.png | 12.5 账户详情与账期 |
| 钱迹-资产-账户每项-更多.png | 12.6 账户更多 |
| 钱迹-记账-支出.png | 8.1、8.2 支出记账 |
| 钱迹-记账-收入.png | 8.1、8.2 收入记账 |
| 钱迹-记账-转账.png | 8.3 转账 |
| 钱迹-记账-收入（支出）添加一级分类.png | 9.2 新增一级分类与图标 |
| 钱迹-记账-收入（支出）一级降级-增加二级分类.png | 9.1、9.2 分类层级管理 |

参考图中出现但用户已明确取消的“报销、分期、存钱计划、意见反馈、关于”不进入开发范围；其余参考页面对应能力均已写入本 PRD。
