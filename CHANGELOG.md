# 更新日志

本项目采用语义化版本号。每个版本除本文件的摘要外，还必须在 `版本记录` 目录保留完整记录，包含本次更新、调整、验证结果、已知问题和下一版本计划。

## [0.3.0] - 2026-08-08

### 新版 PRD 整体可用性重构

- 精简侧边栏与底部导航，重做首页账期、预算卡片、最近汇总、账单日历和组合搜索。
- 预算支持“总预算 + 一级分类预算、仅总预算、仅一级分类预算”三种模式，自动沿用上月配置，并按原交易月份冲减关联退款。
- 分类支持两级结构、图标颜色、排序、归档和统计钻取；搜索结果按日期分组。
- 资产与账户补齐品牌图标、账户资料、信用额度、出账/还款日、账期流水、资产趋势、分组管理和隐藏账户入口。
- 新增分类、账户、资产三类统计视图，提供时间筛选、月度趋势、分布圆环与明细联动。
- 新增多账本创建、切换、改名、归档与恢复；设置页补齐首页偏好、默认账户、主题、备份、恢复、导入和导出入口。
- 快速记账收口为支出、收入、转账三种主入口；信用消费由负债账户自动识别，还款从账户详情发起，退款必须关联原交易并限制累计金额。
- 交易支持最多 3 张本地图片凭证，随数据库备份恢复；完整编辑改为单事务原子替换。
- 数据库升级为 Schema v9，并保持旧库和旧备份兼容。
- 版本统一为 Web `0.3.0`、Android `versionCode 30 / versionName 0.3.0`。

### 验证

- TypeScript、ESLint、Web 生产构建和 22 个测试文件共 182 个测试用例全部通过。
- 按用户要求，本轮暂不执行 Android 构建；需在下一步完成 Capacitor 同步、Debug 构建和真机验收。

完整记录：[版本记录/v0.3.0.md](版本记录/v0.3.0.md)

## [0.2.2] - 2026-08-08

### 复杂手工记账闭环

- 快速记账新增「信用消费」「还款」「退款」三种模式，与原支出、收入、转账形成六种手工入口。
- 信用消费选择信用/负债账户，生成“支出增加 + 负债增加”的复式分录。
- 信用卡还款选择资产还款账户和信用账户，只减少资产与负债，不计入收入或支出。
- 退款选择退款到账账户和原支出分类，增加到账资产或减少信用负债，并冲减当月支出与分类预算。
- 交易详情补充双账户展示；六种手工交易均支持复制和完整编辑，其他系统交易不再错误进入编辑页。
- 修复转账编辑被错误保存成支出的缺陷。
- 搜索筛选分别展示「信用卡还款」和「退款」。
- 包版本、Android `versionCode 22 / versionName 0.2.2`、侧栏版本号和锁文件版本统一。

### 验证

- 20 个测试文件、169 个测试用例全部通过。
- 格式、Lint、TypeScript、Web 生产构建和生产依赖审计通过。
- Android Debug 构建通过，安装包保存在本地 `releases/finance-manager-v0.2.2-debug.apk`。

### 下一版本

- `0.3.0`：通知监听自动记账、周期交易自动入账、系统通知提醒和模板一键应用。

完整记录：[版本记录/v0.2.2.md](版本记录/v0.2.2.md)

## [0.2.1] - 2026-08-04

### 基础交互与体验补全

集中补齐 v0.2.0 发布后发现的基础交互缺失，涵盖侧栏导航、账期切换、交易详情与编辑/删除、账户流水可点击、资产页滚动与借入借出展示等核心体验问题。

### 修复与优化

- **侧栏导航**：首页左上角三横菜单不再跳转到测试页「工程与设计系统机械」，改为从左侧滑出 SideDrawer，包含「我的账本 / 报销管理 / 搜索 / 账单 / 分期 / 周期存钱计划 / 意见与反馈 / 关于 / 设置」共 9 项，其中「我的账本 / 搜索 / 设置」已可跳转，其余标记「规划中」。
- **首页账期切换**：顶部标题改为可点击，弹出账期选择 BottomSheet，支持左右切月、「回到本月」快捷按钮，切换后首页月收支、结余、流水按所选月份重新汇总。
- **首页筛选入口**：右上角 ListFilter 按钮跳转到「搜索页（/search）。
- **首页预算卡片**：接入真实预算数据（BudgetService.getBudgetForPeriod），展示进度条、剩余金额、总额、已用百分比；超支时进度条变红；点击卡片跳转到预算管理页（/budget）。
- **首页交易卡片**：DailyLedgerCard 每笔流水改为可点击按钮，点选后弹出 TransactionDetailSheet 详情弹层。
- **交易详情弹层重构**：改为只读详情视图，表头显示「详情」+ 三个操作按钮（复制 / 修改 / 删除）。第一行左边金额、右边日期；第二行分类；下方账户（可点击跳转账户详情）、「记录于」时间（不可修改）、备注。
- **交易修改流程**：点击「修改」跳转到记账页面（NewExpenseView）进行编辑，可调整金额、分类、账户、备注，时间不可修改（保持原始记录时间），保存时弹确认框；后端通过 voidTransaction + createExpense/Income 实现全字段编辑。
- **交易复制功能**：点击「复制」跳转到记账页面，预填原交易数据，时间默认为当前，保存即创建新交易。
- **交易删除三步确认**：第一步文字确认 → 第二步随机 4 位数字校验，用户必须正确输入数字才能启用删除按钮，防止误删。
- **资产页滚动修复**：移除 section-card__body 去掉 max-height 68dvh 限制，账户列表可完整展开，解决信用卡展开后整体可向上滚动浏览下方卡片。
- **资产页借入借出**：新增 borrow-card 分区，总借入（跳转到 payables）、总借出（跳转到 receivables）分别展示，支持金额与链接。
- **账户流水详情**：AccountDetailView「账户流水」每一行改为可点击按钮，命中后打开 TransactionDetailSheet 查看详情。
- **FinanceService 扩展**：TransactionMetadata 新增 createdAt / categoryId / categoryName / accountId / accountName 字段；新增 editTransactionFull 方法（作废旧交易 + 按原 occurredAt 创建新交易，支持全字段编辑）。
- **formatMinorToCny 工具函数**：domain/money.ts 新增，将分转换为元字符串（如 1800 → "18" / 1850 → "18.50"），供编辑模式预填金额。
- **AccountActivityRecord 接口补 transactionId 字段**：TransactionRepository.listByAccount 改为 SELECT entries.id + transactions.id AS transactionId，保证 AccountDetailView 可关联到原交易。
- **修复 SideDrawer 重复 defineEmits 报错。

### 验证

- 20 个测试文件、165 个测试用例全部通过。
- 格式、Lint、TypeScript、Web 生产构建全部通过。
- APK Debug 构建成功，SHA256：`C1F662578946D54364E2D1D6B003F38895B45B9ABD7B1299692A9583448586A2`。

### 下一版本

- `0.3.0`：通知监听自动记账、周期交易自动入账、系统通知提醒。

完整记录：[版本记录/v0.2.1.md](版本记录/v0.2.1.md)

## [0.2.0] - 2026-08-03

### 日常效率工具集

在 MVP 基础上新增预算管理、模板与周期交易、到期提醒、流水搜索四大日常效率模块，覆盖用户理财高频场景。

### 新增

- **预算管理**：在"我的 → 日常效率 → 预算管理"中按月设置总预算与分类上限，自动汇总当月已发生支出并计算剩余、超支预警。支持新建、编辑、删除预算，查看分类明细与进度条。
- **模板与周期交易**：常用记账可保存为模板（支持支出/收入/转账/信用卡消费/还款/借出/借出收回 7 种类型），一键复用。基于模板可创建周期交易规则，按每日/每周/每月频率与间隔自动计算下次执行时间，到期提示按模板入账。
- **到期提醒**：支持信用卡还款、预付卡到期、应收款到期与自定义四种提醒类型，配置到期日、金额、提前几天提醒。首页"近期到期"按剩余天数排序展示，支持标记已处理。
- **流水搜索**：按关键词（商户/备注/对方）、日期范围、账户、分类、交易类型、金额区间等多维度筛选流水，支持包含已撤销交易，结果按时间倒序展示。
- 新增数据库 migration v8（`008_budget_templates_reminders_v8`）：创建 `budgets`、`category_budgets`、`transaction_templates`、`recurring_transactions`、`reminders` 五张表，含完整索引与约束。

### 文件

- 新增 `src/features/budget/`：预算领域服务、Repository 与单元测试。
- 新增 `src/features/templates/`：模板领域服务、Repository 与单元测试。
- 新增 `src/features/recurring/`：周期交易领域服务、Repository 与单元测试。
- 新增 `src/features/reminders/`：提醒领域服务、Repository 与单元测试。
- 新增 `src/features/search/`：搜索领域服务与 `TransactionRepository.search` 多维筛选。
- 新增 `src/views/BudgetView.vue`、`TemplatesView.vue`、`RemindersView.vue`、`TransactionSearchView.vue` 四个页面。
- 修改 `src/views/ProfileView.vue`：新增"日常效率"分区入口（预算/模板/提醒/搜索）。
- 修改 `src/router/index.ts`：新增 `/budget` `/templates` `/reminders` `/search` 路由。
- 修改 `src/main.ts`：注入 Budget/Template/Recurring/Reminder/Search 五个服务。
- 前端版本更新为 `0.2.0`，Android 更新为 `versionCode 20 / versionName 0.2.0`。

### 验证

- 20 个测试文件、165 个测试用例全部通过（新增 budget 7 + templates 8 + recurring 8 + reminders 10 + search 16 共 49 个测试）。
- 格式、Lint、TypeScript、Web 生产构建全部通过。
- APK Debug 构建成功，SHA256：`A995CDF7D1509CFC1AD77B9F287561F48AECDE39F30ED557DE04A3114BB85BE7`。

### 已知问题（不阻塞发布）

- 周期交易当前仅提示下次执行时间，不会自动入账（需用户手动确认），自动入账在 0.3 版本接入通知监听后实现。
- 提醒仅在应用内展示，不发送系统通知（ Capacitor Local Notifications 接入待 0.3）。
- 预算统计仅含已 posted 状态支出，不包含已撤销交易。
- 小米 17 Pro Max 真机视觉与交互验收待执行。

### 下一版本

- `0.3`：通知监听自动记账、周期交易自动入账、系统通知提醒。

完整记录：[版本记录/v0.2.0.md](版本记录/v0.2.0.md)

## [0.1.0] - 2026-08-03

### MVP 发布版

完成 Sprint 4 剩余项，达到 MVP 发布门槛。

### 新增

- **应用锁（PIN 码）**：在"我的 → 安全 → 应用锁"中设置 4 位 PIN 码。启用后每次打开应用需输入 PIN 解锁。PIN 使用 SHA-256 + 随机盐哈希存储，不保存明文。支持修改 PIN、关闭应用锁、临时禁用。
- **最近任务隐私遮挡**：应用切到后台时自动显示遮挡层，防止系统任务列表预览泄露账目信息。回到前台时如已启用应用锁则要求重新解锁。
- **migration 前自动备份**：数据库 schema 升级前自动生成 JSON 备份到 Documents 目录。migration 失败时提示用户备份路径，可通过备份恢复数据。
- 新增数据库 migration v7（`007_app_settings_v7`）：创建 `app_settings` 表，存储应用锁等设置项。

### 文件

- 新增 `src/features/app-lock/`：应用锁领域服务、store 与单元测试。
- 新增 `src/components/PinPad.vue`：可复用 PIN 输入数字键盘组件。
- 新增 `src/views/PinSetupView.vue`：PIN 码设置/修改/关闭页。
- 新增 `src/views/PinUnlockView.vue`：PIN 码解锁页。
- 修改 `src/App.vue`：启动时 PIN 拦截 + Capacitor App 监听后台/前台隐私遮挡。
- 修改 `src/db/bootstrap.ts`：migration 前自动备份与失败处理。
- 修改 `src/views/ProfileView.vue`、`src/router/index.ts`、`src/main.ts`：集成应用锁入口与注入。

### 验证

- 15 个测试文件、115 个测试用例全部通过（新增 AppLockService 8 个测试）。
- TypeScript 类型检查通过。
- Gradle Debug APK 构建成功。

### 已知问题（不阻塞 MVP）

- 崩溃、断电、低存储极端场景测试待真机验证。
- 小米 17 Pro Max / HyperOS 真机视觉与返回手势验收待执行。

### 下一版本

- `0.2`：预算管理、周期交易、搜索筛选增强、信用卡还款日提醒。

完整记录：[版本记录/v0.1.0.md](版本记录/v0.1.0.md)

## [0.0.13] - 2026-08-03

### 修复

- 修复导入钱迹 CSV 时因"交易类型无法识别"导致近半数行被跳过的问题。当类型关键词无法识别时（如"余额调整"、"代付"、"退款"等），不再直接报错，而是根据**账户数量**推断：有两个不同账户→转账，只有一个账户→根据金额正负推断为支出/收入。
- 修复 `resolveRow` 中转账只有一个账户时直接报"转账必须同时指定转出与转入账户"跳过整行的问题。现在会降级为支出，使用已有账户。
- 修复导入预览页日期只显示年月日不显示时间的问题。现在显示完整日期时间（年月日时分）。

### 验证

- 14 个测试文件、101 个测试用例全部通过（新增"类型无法识别时根据账户数量推断"测试用例）。
- 格式、Lint 检查全部通过。
- APK Debug 构建成功。

### 下一版本

- `0.1.0`：应用锁与隐私处理、migration 前自动备份与回滚、崩溃与低存储测试、真机验收，达到 MVP 发布门槛。

完整记录：[版本记录/v0.0.13.md](版本记录/v0.0.13.md)

## [0.0.12] - 2026-08-03

### 新增

- 新增数据库 migration v5（`005_import_batches_source_v5`），重建 `import_batches` 表，将 `source` 字段的 CHECK 约束从 `('csv', 'qianji', 'other')` 扩展为 `('csv', 'xlsx', 'json', 'qianji', 'other')`，支持导入 Excel 和 JSON 文件。

### 修复

- 修复导入 Excel（.xlsx/.xls）或 JSON 文件时报 `CHECK constraint failed: source IN ('csv', 'qianji', 'other')` 错误的问题。原 v4 migration 的 CHECK 约束未覆盖 `xlsx` 和 `json` 两种来源类型。
- 修复字段映射列表中"日期"和"时间"两个字段同时显示的问题。现在只有当表头中同时存在独立的"日期"列和"时间"列时才显示"时间"字段，单列含完整日期时间（如钱迹"时间"列）应映射到 date。

### 验证

- 14 个测试文件、100 个测试用例全部通过（新增"v5 后允许 xlsx/json 作为 source 值"和"v4→v5 升级保留数据"测试用例）。
- 格式、Lint、TypeScript、Web 生产构建全部通过。
- APK Debug 构建成功。

### 下一版本

- `0.1.0`：应用锁与隐私处理、migration 前自动备份与回滚、崩溃与低存储测试、真机验收，达到 MVP 发布门槛。

完整记录：[版本记录/v0.0.12.md](版本记录/v0.0.12.md)

## [0.0.11] - 2026-08-03

### 调整

- 导入向导 `autoDetectMapping` 增强：无独立"日期"列时，将"时间"列映射到 `date` 系统字段（`parseDate` 已支持 `2026-03-19 13:25:42` 完整日期时间格式）。
- 导入向导 `autoDetectMapping` 增强：识别"账户1/转出账户/付款账户"等表头映射到转出账户，"账户2/转入账户/收款账户"等表头映射到转入账户。
- 导入向导 `autoDetectMapping` 增强：将 ID 列映射到来源交易号（`sourceTransactionId`），用于防重复导入。
- 导入向导 `detectKind` 增强：新增转账关键词识别列表（"还款"、"借出"、"借款"、"借给"、"借入"、"还贷"、"信用卡还款"等），并支持模糊匹配（如"债务-借出"）。
- 导入向导 `parseRow` 增强：当仅填了转出账户且含 `->` / `→` 时，自动拆分为转出/转入账户（如"中信银行->杨浩"）。
- 导入向导 `parseRow` 增强：还款场景下转出/转入同名时（如"中信银行→中信银行"还信用卡），自动将转入方改为"XX信用卡"，避免"同一账户转账"错误。
- 导入向导 `collectPendingAccounts` 增强：当 rawName 包含 catalog 名时（如"中信银行信用卡"包含"信用卡"），用 rawName 作为账户名，保留完整信息。
- 前端版本更新为 `0.0.11`，Android 更新为 `versionCode 11 / versionName 0.0.11`。

### 验证

- 14 个测试文件、98 个测试用例全部通过（新增"识别还款与债务-借出为转账"测试用例）。
- 格式、Lint、TypeScript、Web 生产构建全部通过。
- APK Debug 构建成功，SHA256：`A260BADA86C599E14F5DDC846A88098479EAD2755365D53A986E596975F40B28`。

### 下一版本

- `0.1.0`：应用锁与隐私处理、migration 前自动备份与回滚、崩溃与低存储测试、真机验收，达到 MVP 发布门槛。

完整记录：[版本记录/v0.0.11.md](版本记录/v0.0.11.md)

## [0.0.10] - 2026-08-03

### 新增

- 新增通用 CSV 导入向导（`ImportView`）：选择文件、字段映射、账户/分类映射、预览错误与有效行、确认执行四步流程，支持根据分类或金额自动推断交易类型。
- 新增导入批次管理页（`ImportBatchesView`）：按时间倒序列出批次，支持整批撤销，撤销后交易置为作废且不影响其他批次。
- 新增备份与恢复页（`BackupView`）：生成含 schema 版本、记录数与 SHA-256 校验值的完整 JSON 备份包；恢复前先校验并展示摘要，二次确认后执行，恢复前自动生成当前库备份用于回滚。
- 新增导出页（`ExportView`）：将交易导出为 CSV 或 JSON，可选包含已撤销交易，金额按类型标注正负。
- 新增"我的"页（`ProfileView`）作为数据管理入口，集中放置导入、批次、备份恢复与导出，并展示应用与数据库版本。
- 新增 `ImportService` / `BackupService` / `RestoreService` / `ExportService` 的 Vue 依赖注入键与组合式函数，`main.ts` 统一提供。
- 新增跨平台文件 I/O 辅助（`utils/file-io.ts`）：读取用户选择的文件、在原生平台写入 Documents 目录或在 Web 触发下载。

### 调整

- 同一 CSV 文件重复导入时通过来源指纹命中跳过写入，避免静默重复。
- 数据库 schema 保持 v4（沿用 v0.0.9 的 `import_batches` 表）。
- 路由新增 `import` / `import-batches` / `backup` / `export`，`profile` 由占位页改为 `ProfileView`。
- ESLint 配置对 `.vue` 文件关闭 `no-undef`（vue-tsc 已做类型检查，避免误报浏览器全局类型）。
- 前端版本更新为 `0.0.10`，Android 更新为 `versionCode 10 / versionName 0.0.10`。

### 验证

- 14 个测试文件、96 个测试用例全部通过。
- 格式、Lint、TypeScript、Web 生产构建全部通过。
- 备份生成 → 空库恢复 → 记录数与余额一致性测试通过。
- CSV 导入 → 批次撤销 → 交易作废测试通过。

### 下一版本

- `0.1.0`：应用锁与隐私处理、migration 前自动备份与回滚、崩溃与低存储测试、真机验收，达到 MVP 发布门槛。

完整记录：[版本记录/v0.0.10.md](版本记录/v0.0.10.md)

## [0.0.9] - 2026-08-03

### 新增

- 快速记账页升级为支出 / 收入 / 转账三模式合一，顶部 Tab 切换并按模式动态调整字段与校验。
- 新增借入款独立页面，展示待归还总额、待还/结清数量、债权人、原借入金额、剩余金额和约定还款日。
- 新增借入款登记、部分归还、全部归还与自动结清流程，并生成 `borrowing` / `repay_borrowing` 复式分录。
- 新增账户重命名、归档与取消归档操作；归档前校验账户余额必须为 0。
- 新增账户归档状态可视化（“已归档”标签 + 降低不透明度）。
- 新增数据库 Schema v3 `payables` 表，及 v1 → v3、v2 → v3 无损升级测试。
- 资产页“借入款”入口与“借出款”入口对称展示，点击进入借入款列表。

### 调整

- 扩展复式记账领域模型，正式支持借入款和归还借款交易类型。
- `other_liability` 资产分组标签统一改为“借入款”，与“借出款”对称。
- 账户详情页操作区从单一“调整余额”按钮升级为“调整余额 / 重命名 / 归档”三按钮一行。
- 月收入和月支出继续只统计真实收入/消费，借入/归还和账户转账不混入收支统计。
- 前端版本更新为 `0.0.9`，Android 更新为 `versionCode 9 / versionName 0.0.9`。

### 验证

- 10 个测试文件、59 个测试用例全部通过。
- 格式、Lint、TypeScript、Web 生产构建全部通过。
- v1 → v3、v2 → v3 数据库升级测试通过，原账本数据保留。

### 下一版本

- `0.0.10`：借出款/借入款编辑与删除、快速记账编辑撤销、CSV 导入适配器、备份恢复闭环。

完整记录：[版本记录/v0.0.9.md](版本记录/v0.0.9.md)

## [0.0.8] - 2026-08-03

### 新增

- 新建资金、信用、充值和投资账户时可录入当前余额，并生成可审计的期初余额分录。
- 新增账户详情页，展示品牌、机构、当前余额和按时间倒序排列的账户流水。
- 新增余额调整流程，只记录实际余额与账面余额的差额，不覆盖历史交易。
- 新增借出款独立页面，展示待收总额、待收/结清数量、借款人、原借出金额、剩余金额和约定还款日。
- 新增借出款登记、部分还款、全部还款与自动结清流程。
- 新增数据库 Schema v2 `receivables` 表及 v1 → v2 无损升级测试。

### 调整

- 扩展复式记账领域模型，正式支持期初余额、余额调整、借出款和收回款交易类型。
- 资产页始终展示借出款入口，账户分组行可进入账户详情。
- 月收入和月支出继续只统计真实收入/消费，期初余额、调账和借出/收回不混入收支统计。
- 前端版本更新为 `0.0.8`，Android 更新为 `versionCode 8 / versionName 0.0.8`。

### 验证

- 10 个测试文件、49 个测试用例全部通过。
- 格式、Lint、TypeScript、Web 生产构建和 Android Debug 构建全部通过。
- 小米真机完成数据库升级、期初余额表单、账户详情和借出款页面验收。

### 下一版本

- `0.0.9`：借入款/归还借款、收入记账、账户转账和账户归档。

完整记录：[版本记录/v0.0.8.md](版本记录/v0.0.8.md)

## [0.0.7] - 2026-08-03

### 新增

- 首页与资产页接入 Vant Swipe，首页向左滑进入资产总览，资产页向右滑返回首页。
- 新增净资产、总资产、总负债、资产/负债数量、负债率、总借入和总借出聚合。
- 新增信用卡、资金、充值、投资分组汇总，并为借出款和其他负债预留动态分组。
- 新增右下角 56px 圆形添加资产按钮和四分区账户分类底部弹层。
- 新增 38 个资金、信用、充值和投资账户入口，统一使用 48px 彩色品牌圆标。
- 新增 19 家银行目录、搜索与二级选择流程；银行卡和信用卡可自动带入银行名称与机构。
- 新增账户分组明细弹层、金额隐藏和统一品牌图标组件。
- 新增资产汇总纯函数及自动化测试。

### 调整

- `FinanceService.createAccount` 扩展为支持全部账户类型，不再仅限现金、银行卡和平台账户。
- 所有底部弹层统一 Teleport 到 `body`，避免在水平滑动容器内受到裁切。
- 资产模块 UI 规范补充顶部摘要、卡片顺序、添加按钮、目录网格和银行列表尺寸。
- 前端版本更新为 `0.0.7`，Android 更新为 `versionCode 7 / versionName 0.0.7`。

### 验证

- 10 个测试文件、44 个测试用例全部通过。
- 格式、Lint、TypeScript、Web 生产构建和 Android Debug 构建全部通过。
- 小米真机完成首页左滑、资产总览、账户目录、银行列表和账户表单验收。

### 下一版本

- `0.0.8`：账户期初余额、账户详情流水、借出款/收回款和借入款管理。

完整记录：[版本记录/v0.0.7.md](版本记录/v0.0.7.md)

## [0.0.6] - 2026-08-03

### 调整

- 首页摄影区在 v0.0.5 基础上增加 `50px`，最终固定高度为 `246px`。
- 重新对照钱迹首页：月支出恢复为单独的主信息区，金额使用 28px 强调字号。
- 月收入与本月结余位于下一行，每项采用“标题 + 数值”的横向组合。
- 摄影区全部指标标题和金额统一提升为 600 字重，标签白色不透明度提高到 92%。
- 摘要区距摄影图底部 16px，次级指标间距统一为 24px。
- 同步更新小米 17 Pro Max UI 规范中的高度、占屏比例和参考输出尺寸。
- 前端版本更新为 `0.0.6`，Android 更新为 `versionCode 6 / versionName 0.0.6`。

### 验证

- 9 个测试文件、42 个测试用例全部通过。
- 格式、Lint、TypeScript 和 Web 生产构建全部通过。
- 小米真机候选布局截图确认：摄影区、主月支出、次级收入/结余均与参考层级一致。

### 下一版本

- `0.0.7`：补齐收入、转账、负债账户与借出款（别人欠我钱）管理，并提供账户详情。

完整记录：[版本记录/v0.0.6.md](版本记录/v0.0.6.md)

## [0.0.5] - 2026-08-03

### 调整

- 首页摄影头图由 `296px` 精确缩短 `100px`，新高度为 `196px`。
- 月支出、月收入和本月结余改为同一行三栏摘要，适配缩短后的摄影区。
- 月支出继续保留一级金额强调，其余两项使用统一列表金额规格。
- UI 权威规范同步更新 `HomeHero` 高度、占屏比例和输出参考尺寸。
- 前端版本更新为 `0.0.5`，Android 更新为 `versionCode 5 / versionName 0.0.5`。

### 验证

- 9 个测试文件、42 个测试用例全部通过。
- 格式、Lint、TypeScript 和 Web 生产构建全部通过。
- Android Debug 构建与小米真机 ADB 截图验收结果见完整版本记录。

### 下一版本

- `0.0.6`：补齐收入、转账、负债账户与借出款（别人欠我钱）管理，并提供账户详情。

完整记录：[版本记录/v0.0.5.md](版本记录/v0.0.5.md)

## [0.0.4] - 2026-08-03

### 新增

- 新增正式首页：摄影头图、当月收入/支出/结余、预算占位卡片、按日流水与空状态。
- 新增底部五栏主导航，并统一 56px 顶部导航、64px 底部导航和 56px 中央记账按钮。
- 新增资金账户页，可创建现金、银行卡和平台账户并读取实时余额。
- 新增快速支出页，支持金额、付款账户、支出分类、消费时间、商家和备注。
- 新增月度汇总、当月流水查询 Repository 和 FinanceService 业务门面。
- 新增人民币输入转整数分规则、业务闭环集成测试和流水组件测试。
- 新增原创山水摄影风格首页头图，并纳入本地应用资源。

### 调整

- 默认入口由工程组件基线页切换为正式首页，工程页保留在 `/foundation`。
- 前端版本更新为 `0.0.4`，Android 更新为 `versionCode 4 / versionName 0.0.4`。
- 首页、资产、分析、我的统一使用同一套路由壳、Design Token、Lucide 线性图标和页面过渡。

### 验证

- 9 个测试文件、42 个测试用例全部通过。
- 格式、Lint、TypeScript 和 Web 生产构建全部通过。
- Android Debug 构建通过；在小米设备 `2509FPN0BC` 覆盖安装并完成首页、资产页和快速支出页 ADB 验收。

### 下一版本

- `0.0.5`：补齐收入、转账、负债账户与借出款（别人欠我钱）管理，并提供账户详情。

完整记录：[版本记录/v0.0.4.md](版本记录/v0.0.4.md)

## [0.0.3] - 2026-08-03

### 新增

- 新增 `LedgerRepository`、`AccountRepository`、`CategoryRepository` 与 `TransactionRepository`。
- 新增带参数 SQL 批处理接口，保证交易与全部分录在同一个 SQLite 事务内写入。
- 新增统一实体 ID 生成器、UTC 时间规范和持久化记录类型。
- 首次启动自动创建“日常账本”、现金账户、8 个支出分类和 5 个收入分类。
- 新增账户余额、账本流水、交易详情与交易数量查询。
- 新增关闭并重开真实 SQLite 文件后的持久化回归测试。
- 增加本地真机验证截图目录，并从 Git 历史中排除验证产物。

### 调整

- 前端版本更新为 `0.0.3`，Android 更新为 `versionCode 3 / versionName 0.0.3`。
- 组件基线页数据库状态增加“账本已初始化”提示。
- 补记 `v0.0.2` 在小米真机安装和 SQLite schema v1 初始化通过。

### 验证

- 6 个测试文件、28 个测试用例全部通过。
- 格式、Lint、TypeScript、Web 生产构建、生产依赖审计和 Android Debug 构建全部通过。
- 通过 ADB 在小米设备 `2509FPN0BC` 覆盖安装、启动、强制停止并重启；数据库文件持续存在，未发现崩溃或 SQLite 异常。

### 下一版本

- `0.0.4`：进入 Sprint 1，实现账户创建、第一笔手工支出、真实月度汇总和按日流水首页。

完整记录：[版本记录/v0.0.3.md](版本记录/v0.0.3.md)

## [0.0.2] - 2026-08-03

### 新增

- 接入 `@capacitor-community/sqlite` 8.1.0，并同步到 Android 原生工程。
- 新增 SQLite schema v1：账本、账户、分类、交易、分录和 migration 记录表。
- 新增账户余额、交易借贷平衡视图，以及索引、外键、金额和账户方向约束。
- 新增原子 migration runner，支持重复启动、失败回滚和未知高版本拒绝降级。
- 新增账户类型、正常余额方向、整数分金额和交易分录领域类型。
- 实现支出、收入、账户转账和信用消费四类复式分录规则。
- Android 启动时初始化数据库；组件基线页显示 SQLite 状态与 schema 版本。
- 新增基于真实 SQLite 引擎的 schema/migration 集成测试。

### 调整

- 前端版本更新为 `0.0.2`，Android 更新为 `versionCode 2 / versionName 0.0.2`。
- 本地工程迁移到纯英文路径 `D:\finance-manager`，避免 Android/Gradle 工具链处理中文路径时出现资源合并异常。

### 验证

- 4 个测试文件、21 个测试用例全部通过。
- 格式、Lint、TypeScript、Web 生产构建和 Android Debug 构建全部通过。
- 生产依赖安全审计为 0 个已知漏洞。

### 下一版本

- `0.0.3`：Repository 持久化层、账本/账户/分类初始化服务、交易与分录原子写入和余额查询测试。

完整记录：[版本记录/v0.0.2.md](版本记录/v0.0.2.md)

## [0.0.1] - 2026-08-03

### 新增

- Vue 3 + TypeScript + Vite + Capacitor Android 工程基线。
- UI V3 Design Token 和第一批复用组件。
- Pinia、Vue Router、Vant Popup 与 Lucide 图标接入。
- Vitest、Vue Test Utils、ESLint、Prettier 和生产构建脚本。
- 开发组件基线页。
- 版本记录制度与发布检查清单。
- 首个可安装的 Android Debug APK。

### 调整

- Vant 样式改为按组件引入，减少无用 CSS。
- Android `versionName` 与前端版本统一为 `0.0.1`。
- 增加 Windows 中文项目路径的 Android Gradle 兼容设置。

### 下一版本

- `0.0.2`：SQLite 接入、schema v1、migration runner、账务领域类型与第一组分录规则测试。

完整记录：[版本记录/v0.0.1.md](版本记录/v0.0.1.md)
